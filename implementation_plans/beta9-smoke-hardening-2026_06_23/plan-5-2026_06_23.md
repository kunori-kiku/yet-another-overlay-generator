# plan-5 — Release `v2.0.0-beta.10`

> Part of [outline.md](./outline.md). Release mechanics only — mirrors the beta.9 runbook (memory: `agent-feedback-version-rollout-shipped`).

## Prerequisites
**plans 1–4 ALL merged to `main` and green.** No new code — ships what plans 1–4 built.

## Goal
Roll the CHANGELOG, cut + push the annotated `v2.0.0-beta.10` tag, verify the published release, promote it to GitHub **Latest** (like beta.9), and smoke the new behavior.

## Read first
1. `.github/workflows/release.yml` (gates mirror ci.yml's REQUIRED set — already fixed in beta.9 PR #173 to use `VITE_E2E=1` build + `--grep-invert "visual corpus"`; `make_latest:` is false for `-beta.`; the `-X main.BuildVersion` stamp), `CHANGELOG.md` (the `[Unreleased]` header + the beta.9 entry + the compare-link footer), `cmd/server/main.go:79` (BuildVersion).
2. Memory: the beta.9 release lessons (promote-to-Latest clears the prerelease flag; CHANGELOG covers the whole `prev..HEAD` delta).

## Implementation steps
1. Pre-flight: confirm plans 1–4 merged (`git log v2.0.0-beta.9..HEAD`); CI green on the `main` tip; reproduce the `-X main.BuildVersion=v2.0.0-beta.10-localtest` stamp → `yaog-server version` prints it; local suite (`gofmt -l`, `go vet ./...` + airgap, `go test -race ./...` + airgap, FE lint + build).
2. Roll `CHANGELOG.md`: a dated `## [2.0.0-beta.10]` section below an empty `## [Unreleased]`, covering the whole `v2.0.0-beta.9..HEAD` delta (live telemetry monitoring channel; controller-mode Validate fix; off-host signing-handle auto-recovery; mimic discover-and-pick). Update the compare-link footer. Reviewed CHANGELOG PR → merge → re-confirm `main` green at the tip.
3. Cut the **annotated** tag from `main` (project identity `kunori-kiku <rokuyanlin@gmail.com>`, no AI attribution); push → triggers `release.yml` + `docker.yml`.
4. Watch both pipelines green. Verify assets (7 bundles, agent linux/windows + `.sha256` sidecars, airgap servers, local-design zip).
5. **Promote to Latest** (owner's standing preference, like beta.9): `gh release edit v2.0.0-beta.10 --latest --prerelease=false` (clears the prerelease flag, demotes beta.9). Confirm `releases/latest/download` resolves to beta.10.
6. Smoke: published `yaog-server`/`yaog-agent version` → `v2.0.0-beta.10`; agent `.sha256` verifies; **the telemetry heartbeat** — start an agent against a controller, confirm `node.conditions` refresh live (ObservedAt advances) without a deploy and `wireguard` flips to `AllPeersUp` after handshakes settle.

## Tests produced by this plan
None (release mechanics). The CI suite + `release.yml` gates re-run on the tagged commit.

## Definition of done
- [ ] plans 1–4 merged; `main` green; build-stamp reproduced; local suite green.
- [ ] CHANGELOG rolled (full delta) + PR reviewed + merged.
- [ ] Annotated `v2.0.0-beta.10` tag pushed; `release.yml` + `docker.yml` green; all assets + sidecars present.
- [ ] Promoted to Latest (`isLatest=true`, `isPrerelease=false`); `releases/latest/download` → beta.10.
- [ ] Smokes recorded: version stamp + agent sidecar verify + **live telemetry heartbeat** (conditions refresh without a deploy).
- [ ] STATUS.md + outline status table updated (plan-5 delivered, subject delivered); memory note appended; subject archived to `_completed/`.

## Out of scope
- The `v2.0.0-rc.1` cut (owner-gated, separate).

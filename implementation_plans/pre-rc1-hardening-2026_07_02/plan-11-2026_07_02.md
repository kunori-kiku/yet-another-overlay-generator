# plan-11 — rc.1 gate refresh + release

> See `outline.md` (§7 plan-11). The terminal rc.1 cut + hardware smokes + branch protection remain
> owner-gated; this plan makes every automatable criterion green and refreshes the gate doc.

## Prerequisites
plans 1–10 merged to `main`, CI green. beta.17 shipped + promoted.

## Goal
Refresh `docs/spec/rc1/RC1-GATE.md` to record this subject's fixes; confirm criteria A–E; execute the
release runbook to cut `v2.0.0-rc.1` (owner terminal actions: hardware smokes, branch protection,
signature).

## Reads from specs
`(none — release + gate paperwork)`

## Read first (exact)
- `docs/spec/rc1/RC1-GATE.md` (the go/no-go SSOT — criteria A–E, required-checks set, runbook,
  sign-off), `docs/spec/rc1/RUNBOOK.md` (owed hardware smokes).
- `RELEASING.md` (release mechanism + owner `--latest` override for rc.1), `CHANGELOG.md`.
- `.github/workflows/ci.yml`, `release.yml`, `docker.yml` (the required jobs).

## Implementation steps

### Step 1 — refresh RC1-GATE.md
Add this subject's fixes to the criteria evidence: criterion **A** (blocker-clean) records the two
criticals fixed with regression coverage (plan-1 NAT, plan-2 self-update keystone) + the four audited
security scopes (plans 4–7) with their tests; note the three functionality additions (plans 8–10).
Confirm **B** (gates green + required), **C** (owed smokes — the manual NAT smoke the owner ran is now
fixed; add a "NAT override honored" line item), **D** (unchanged accepted assumptions + the rc.2
deferrals from §6), **E** (release mechanics). Keep the owner sign-off block.

### Step 2 — roll CHANGELOG for rc.1
Move `## [Unreleased]` → `## [2.0.0-rc.1] - <date>`; fresh empty `## [Unreleased]`; compare link.
Section covers `beta.17..HEAD` (the hardening plans 4–10 + this gate refresh). Confirm with
`git log v2.0.0-beta.17..HEAD --oneline`.

### Step 3 — local verify (both profiles + FE + conformance)
```
cd /home/kunorikiku/source/yet-another-overlay-generator
$HOME/.local/go/bin/gofmt -l . ; \
$HOME/.local/go/bin/go build ./... && $HOME/.local/go/bin/go vet ./... && \
  $HOME/.local/go/bin/go vet -tags airgap ./... && \
  $HOME/.local/go/bin/go test -race ./... && $HOME/.local/go/bin/go test -tags airgap ./...
cd frontend && npm run lint && npm run build && npx vitest run
```

### Step 4 — release (owner terminal actions surfaced)
Per `RC1-GATE.md` §Release runbook: annotated tag `v2.0.0-rc.1` from green `main`; publish `--latest`
(owner override for rc.1); watch `release.yml` + `docker.yml`; verify `isLatest: true` + `version`
prints `v2.0.0-rc.1`. Owner retains: the three RUNBOOK §C1/§C2/§C3 hardware smokes (or accept-risk),
branch protection on the required-checks set, and the go/no-go signature.

### Commit
```
git add -A && git commit -m "$(cat <<'EOF'
docs(rc1): refresh RC1-GATE for pre-rc1-hardening; roll CHANGELOG for v2.0.0-rc.1 (plan-11)

Claude-Session: https://claude.ai/code/session_01H97xU5jLNALVJfvpVLFJma
EOF
)"
```

## Tests produced by this plan
None (paperwork + release). All regression coverage ships in plans 1–10.

## Definition of done
- [ ] `RC1-GATE.md` records the subject's fixes across criteria A–E; sign-off block intact.
- [ ] CHANGELOG rolled to `2.0.0-rc.1`; compare link.
- [ ] Local verify (both profiles + FE + conformance) green; every automatable rc.1 criterion green.
- [ ] Release runbook executed to the owner-gated boundary; the owner terminal actions clearly surfaced.
- [ ] The gate/CHANGELOG PR independently reviewed → CI green → merged before the tag.

## Out of scope
- The owner-only terminal actions (hardware smokes, branch protection, tag signature) — surfaced, not
  performed.
- The rc.2 deferrals (FileStore backup/restore+HA; blind-digest-signing; attacker-resistant
  anti-rollback; bootstrap-cred OOB delivery).

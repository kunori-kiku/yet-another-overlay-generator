# plan-6 — Closure & v2.0.0-beta.3 release

See `outline.md`. Milestone: closure + release. **Prerequisites:** plan-1…plan-5 all `done`.

## Goal
Make the specs/docs honest about the now-built panel rollout UI, flip the prior subject's descope record
to delivered, roll the CHANGELOG/STATUS, archive this subject, and cut **v2.0.0-beta.3** (tag user-gated).

## Reads from specs
`panel-deploy-fleet`, `panel-shell`, `controller-operator-api`.

## Read first
- `docs/spec/controller/agent-selfupdate.md:158-166` (§Panel scope — flip from "documented follow-up"
  to "built").
- `docs/spec/api/wire-contract.md` (add the rollout + mimic settings fields + the `release-pins`
  endpoint + nodeJSON `in_rollout`/`target_agent_version` to the parity tables).
- `specs/panel-deploy-fleet.md`, `specs/panel-shell.md` (record the config cards + status surface; per
  close-phase Step 4.6 / refresh-specs).
- `implementation_plans/_completed/signed-self-update-and-rc-hardening-2026_06_15/CLOSURE.md:96-110`
  (the "Descoped deliverables" note — mark the Canary UI delivered) and repo-root `STATUS.md`.
- `CHANGELOG.md` (head), the last subject's plan-8/plan-10 (release ritual: bookkeeping PR → annotated
  tag → release.yml auto-creates the GitHub release + assets → `gh release edit --notes-file --latest`).
- `.github/workflows/release.yml` (the tag-triggered build/release).

## Implementation steps

1. **Specs honesty:** flip `agent-selfupdate.md` §Panel scope to describe the shipped cards + per-node
   chip (remove "documented follow-up, not a beta.2 blocker"). Add the new settings fields + the
   `release-pins` endpoint + the nodeJSON additions to `wire-contract.md`'s parity tables. Update
   `specs/panel-deploy-fleet.md` (+ `panel-shell.md` if a sub-section changed).

2. **Descope record → delivered:** in the prior subject's `CLOSURE.md` "Descoped deliverables" note,
   mark the plan-9 Canary UI **delivered by `controller-panel-rollout-ui-2026_06_16`** (don't delete the
   history; annotate it). Mirror in repo-root `STATUS.md` (move it out of owed/open into the delivery
   record).

3. **CHANGELOG + STATUS:** add a `[2.0.0-beta.3]` section (agent rollout + mimic catalog config UI,
   per-node update-status chip + opt-in live poll, the `release-pins` assisted-fetch endpoint + SSRF
   guards, the full-replace drop-on-save fix, nodeJSON `in_rollout`). Regenerate STATUS "Active work" /
   "Recently closed subjects".

4. **Bookkeeping PR:** commit the docs/CHANGELOG/STATUS; open + review + merge per the standing loop.

5. **Archive:** `git mv implementation_plans/controller-panel-rollout-ui-2026_06_16
   implementation_plans/_completed/` with a `CLOSURE.md` (shipped table, PR list, the assisted-fetch
   custody argument, what's deferred), in the bookkeeping PR (or a follow-up) per close-phase.

6. **Release:** after merge to `main`, push annotated tag `v2.0.0-beta.3`; let `release.yml` build +
   create the GitHub release + attach assets; then `gh release edit v2.0.0-beta.3 --notes-file <notes>
   --latest`. **Tag push is user-gated** — confirm before tagging.

## Verification
- All PRs green; `release.yml` run green; `gh release view v2.0.0-beta.3` shows assets + `--latest`.
- `rg` finds no remaining "documented follow-up"/"not built" claim for the Canary UI in specs.

## Tests produced by this plan
- None (docs/release only).

## Definition of done
- Specs honest (Panel scope built; wire-contract + panel specs updated); CLOSURE/STATUS mark the Canary
  UI delivered; CHANGELOG rolled; subject archived to `_completed/`; `v2.0.0-beta.3` released as latest
  (after user go-ahead on the tag).

## Out of scope
- Any new feature work. The owner-owed manual smokes / rc.1 tagging from the prior subject (unchanged;
  separate owner calls).

# Plan 3 — Frontend label + docs sync (PR)

Outline: [outline.md](outline.md) · Prerequisites: plan-2 merged · Branch stacked on plan-2.

## Goal

Surface the now-real `tcp` transport honestly in the UI and docs. Small, no new state (keyless).

Reads from specs: api/wire-contract, data-model/edge, artifacts/mimic

## Read first

- `frontend/src/components/layout/RightPanel.tsx:828-840` (the transport `<select>`),
  `frontend/src/i18n.ts` (STRINGS), `docs/wiki.md` / `docs/wiki-zh.md` (transport mentions).

## Implementation steps

1. **Relabel the transport option** — the `tcp` `<option>` label becomes "TCP (mimic)" (value stays
   `tcp`). Add a short hint shown when an edge's transport is `tcp`: works on networks that throttle
   or block UDP; requires both endpoints on Linux with eBPF; MTU is auto-lowered; **not a
   censorship-circumvention feature**. i18n via STRINGS tuples (zh/en), matching existing style.
2. **No secret UI** — mimic is keyless; nothing to display or pin. Do not add any secret field.
3. **Docs sync** — `docs/wiki.md` + `docs/wiki-zh.md`: document the `tcp`/mimic transport with the
   honest positioning and the both-ends-Linux + MTU notes; cross-reference `docs/spec/artifacts/mimic.md`.

Commit: `feat(frontend): label tcp transport as "TCP (mimic)" with honest UDP-restriction hint`

Manual smoke (no FE runner): select an edge → transport dropdown shows "TCP (mimic)" → choosing it
shows the hint → compile → (with plan-2) the node's install bundle carries the mimic phase.

## Tests produced

None executable (no FE runner). CI gates: tsc + eslint + build. Manual smoke in the PR description.

## Definition of done

- [ ] CI green; local `npm run lint && npm run build` clean.
- [ ] No anti-censorship language anywhere in UI/docs; positioning is UDP-restriction only.

## Out of scope

Backend (plan-2); any keyed/secret UI; censorship features.

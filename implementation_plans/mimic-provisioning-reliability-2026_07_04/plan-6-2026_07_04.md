# plan-6 — Release v2.0.0-rc.2

**Prerequisites:** plans 1–5 merged; `main` green. Branch `chore/changelog-rc2` off `main`.

**Goal:** roll the CHANGELOG for the `v2.0.0-rc.1..HEAD` delta and cut `v2.0.0-rc.2` as GitHub Latest
(rc self-promotes — `make_latest` is true for non-`-beta.`/`-preview.` tags).

**Read first:** `CHANGELOG.md` (the rc.1 entry + compare-link footer), `RELEASING.md`,
`.github/workflows/release.yml` (the `make_latest` expression + the gate-e2e set),
`docs/spec/rc1/RC1-GATE.md` (gate rules still apply to an rc bump).

## Implementation steps

### 1. CHANGELOG (`CHANGELOG.md`)
Add a `v2.0.0-rc.2` entry for the `rc.1..HEAD` delta:
- **Fixed:** mimic `transport: tcp` failed to install on Debian 12 / Ubuntu 24.04 — the catalog now
  pins + installs both the `mimic` and `mimic-dkms` packages (the `mimic-modules` dependency); a
  mimic provisioning failure under `mimic_fallback: udp` now degrades to plain UDP instead of aborting
  the node apply (and writes a mimic Node Condition under fail-closed).
- **Added:** native XDP auto-downgrades to skb when the NIC can't attach, with the achieved mode
  surfaced per node; a pre-deploy native-XDP capability indicator + warning; Discover pairs the
  `-dkms` asset to its row and Assist fetches both sidecars.
- Add the compare link; if adopting the deferred fix, also add the missing beta.10–16 footer links
  (rc.2 backlog item) — decide with the CHANGELOG review.

### 2. Verify + tag
- Full local suite: `go build ./... && go build -tags airgap ./... && go vet ./... && go test -race
  ./... && go test -tags airgap ./...`; `cd frontend && npm run lint && npm run build && npx vitest
  run`; conformance + drift green.
- Reviewed CHANGELOG PR → merge → confirm `main` green (all six required checks).
- Annotated tag `v2.0.0-rc.2` on the green tip → push. `release.yml` runs; because the tag has no
  `-beta.`/`-preview.` substring, `make_latest=true` → **self-promotes to Latest** (no manual
  `gh release edit`). Verify: `gh release list` shows rc.2 as Latest; 29 assets incl. per-arch
  `yaog-agent-*` + `.sha256` sidecars; download a sidecar + `sha256sum -c`; the published
  `yaog-server`/`yaog-agent` `version` prints `v2.0.0-rc.2`. `docker.yml` green (async).
  (gh caveat: `--json isLatest` is invalid in this gh — use the `gh release list` Latest column.)

### 3. Closeout
- Refresh `STATUS.md`: rc.2 released; the mimic-provisioning subject CLOSED; next = continue the rc
  soak / GA road.
- Update memory: a new `mimic-provisioning-reliability-shipped` project memory + the `MEMORY.md`
  pointer; note the two-package install lesson + the robust-fallback contract.
- `git mv implementation_plans/mimic-provisioning-reliability-2026_07_04
  implementation_plans/_completed/` → commit + push (the closure PR).
- Hand the owner the smoke script: set a fleet accelerator edge to `transport: tcp` with the catalog
  populated (both debs), deploy; confirm (a) a current-kernel node installs both debs and mimic
  activates, (b) a stale-kernel node with policy=udp comes up as plain UDP with a mimic Node
  Condition (not a stuck apply), (c) a native-XDP node auto-downgrades to skb + the panel shows the
  achieved mode + the pre-deploy capability indicator.

## Definition of done
`v2.0.0-rc.2` is GitHub Latest, verified; STATUS + memory refreshed; subject archived to
`_completed/`; owner handed the smoke script.

## Out of scope
Any new code behavior (this is the release plan — a defect found here files a plan-N.5 against the
relevant earlier plan, it is not patched in the release branch).

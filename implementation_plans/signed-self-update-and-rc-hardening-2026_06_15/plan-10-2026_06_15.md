# plan-10 — beta.2 closure & release + subject close

See `outline.md`. Milestone: **beta.2**. Prerequisites: plan-9 merged. This CLOSES the subject.

## Goal
Field-validate the self-update, cut **beta.2** (latest), and run the full close-phase ritual.

## Reads from specs
`controller/agent-api`, `controller/bootstrap`; `implementation_plans/README.md` (closure ritual).

## Read first
- `STATUS.md`; `PRINCIPLES.md` (confirm plan-9's self-update custody amendment landed); the outline status table.
- `RELEASING.md`.

## Implementation steps
1. **Self-update field smoke (gate the TAG):** publish a new agent version to a canary node; watch it
   download → verify → swap → re-exec → badge flip to the new version; promote canary → fleet; then publish
   a TAMPERED hash and confirm the node refuses + keeps last-good; confirm a forced-crash binary rolls back
   within the attempt cap. Record results (or owed) in STATUS.md/CLOSURE.md.
2. **Tag beta.2:** annotated tag (e.g. `v2.0.0-beta.2`), push, `gh release create … --latest` with notes
   (signed self-update + canary rollout).
3. **close-phase (subject):** outline status table all-done; regenerate `STATUS.md` (subject delivered;
   rc.1 a later owner call once beta soak clean + the two beta.1 hardware smokes pass); write `CLOSURE.md`
   (shipped table beta.1+beta.2, the signed-`artifacts.json` trust argument, what's deferred: bootstrap-TOFU
   hole + FileStore SPOF fix → rc.2/GA); migrate subject-scoped tests per the test-lifecycle taxonomy;
   `git mv` the subject folder → `implementation_plans/_completed/`; commit + push.
4. **Memory:** update [[agent-self-update-signed-verification]] → SHIPPED.

## Tests produced
- None new (release/bookkeeping). Self-update smoke is a manual record.

## Definition of done
beta.2 tagged + set `latest`; self-update field smoke run or recorded owed; PRINCIPLES.md self-update HIGH
principle present; CLOSURE.md written; subject archived to `_completed/`; STATUS.md regenerated; memory updated.

## Out of scope
rc.1/GA tagging (later owner call); the deferred rc.2 items (bootstrap-TOFU close, FileStore SPOF fix, full
wiki rewrite, FE test runner).

# plan-8 — beta.1 closure & release

See `outline.md`. Milestone: **beta.1**. Prerequisites: plans 1–7 all merged to main.

## Goal
Exercise the headline paths once, cut **beta.1** (set as the latest GitHub release), and update project
state. This is a *partial* close — the subject stays open for beta.2 (the self-update swap).

## Reads from specs
`controller/stage-promote`, `panel-auth`, `artifacts/mimic`.

## Read first
- `STATUS.md`; `RELEASING.md` (from plan-1); `git tag --list --sort=-v:refname` (latest = `v2.0.0-preview.10`).
- The two owed-smoke records: `STATUS.md:24,28`.

## Implementation steps
1. **Hardware smokes (gate the TAG, not code-merge):**
   - Two-node controller: cache-clear → WebAuthn login (use `http://localhost`, not 127.0.0.1) → hydrated
     canvas; persisted-controller full-page login; controller→local warns+preserves+purges; fleet "not in
     design" markers + one-click revoke; login survives refresh; no token in localStorage.
   - NAT sticky-pin round-trip: Compile → edit internal port + transit IP on a port-restricted NAT VPS →
     Save → deploy → agent listens on the chosen port/IP → survives a re-deploy with no drift.
   - mimic GitHub-`.deb` install on a real kernel-≥6.1 Debian host (per mimic.md Verification).
   - If hardware is unavailable: record as **owed** in STATUS.md + the beta.1 notes (same convention as the
     existing owed smokes) — owner-accepted risk.
2. **Tag beta.1:** version per `RELEASING.md` (e.g. `v2.0.0-beta.1`); annotated tag (env identity); push;
   `gh release create … --latest` with notes covering #98–#106 closure + this subject's beta.1 scope (mimic
   GitHub install, version reporting, full validator, controller-mode UX, RC paperwork).
3. **State:** regenerate `STATUS.md` — subject `signed-self-update-and-rc-hardening` **partially delivered**
   (beta.1 shipped; beta.2 = self-update swap pending); mark plans 1–8 done in the outline status table;
   decisions-log entry. (No subject archive yet — beta.2 remains.)
4. Commit; push.

## Tests produced
- None new (release/bookkeeping). The smokes are manual records, not automated tests.

## Definition of done
beta.1 tagged + GitHub release set `latest`; smokes run or recorded owed; STATUS.md + outline reflect
beta.1-shipped / beta.2-pending; CHANGELOG `[Unreleased]` rolled to the beta.1 section.

## Out of scope
The self-update swap (plan-9); subject archive + CLOSURE.md (plan-10).

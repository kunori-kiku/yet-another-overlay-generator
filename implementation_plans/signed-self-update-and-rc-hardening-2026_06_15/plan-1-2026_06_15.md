# plan-1 — RC paperwork, format gate & trust-doc honesty (Unit A)

See `outline.md`. Milestone: **beta.1**. Prerequisites: none (pure docs/format/bookkeeping; runs fully
parallel to plans 2–9; stack against any). Branch: continue on `rc-hardening` (LICENSE/NOTICE already
there) or a `plan-1-*` branch off it.

## Goal
Close the non-code RC blockers and make the project's own docs honest, so the build reads as trustworthy.
No behavioral code change.

## Reads from specs
`compiler/validation` (the coverage contract being de-staled).

## Read first
- `LICENSE`, `NOTICE` (already added on `rc-hardening` — verify present).
- `README.md:30-31` (Go 1.21+/Node v18+ — both stale), `:174-175,179` (wiki "Full documentation" links).
- `docs/wiki.md`, `docs/wiki-zh.md` (predate Controller Mode 2.0).
- `docs/spec/compiler/validation.md` (`none-yet` rows + dead "Closed by Plan 3/5/9" refs at ~`:53,:94,:101,:112,:139,:146`).
- `gofmt -l ./cmd ./internal` → the 13 drift files (incl. `internal/compiler/compiler.go`, `roles.go`,
  `internal/controller/memstore.go`, `internal/renderer/escape.go`, `sysctl.go` + 8 test files).
- `.github/workflows/ci.yml`; `implementation_plans/controller-panel-2026_06_08/` (15 files, no CLOSURE.md).

## Implementation steps
1. **CHANGELOG.md** (new, Keep-a-Changelog style): seed `[Unreleased]` + back-fill the shipped programs
   (#59–#106) at a release-note granularity. **RELEASING.md** (new): document preview→beta→rc→GA, annotated
   tags, `gh release` "set latest", the `-X` ldflags build-version convention (forward-ref plan-4).
2. **README version drift:** Go `1.21+`→`1.25+`; Node `v18+`→`v20+` (reconcile with `PRINCIPLES.md:49`).
3. **Wiki redirect:** point `README.md:174-175,179` "Full documentation" at `docs/spec/controller/*`; add an
   air-gap-scope banner to the TOP of BOTH `docs/wiki.md` and `docs/wiki-zh.md` ("covers air-gap mode;
   controller mode → docs/spec/controller/"). (Addition 1a.)
4. **validation.md de-stale (Addition 1c):** flip ONLY already-shipped rows to `schema`/`semantic`; EXCISE
   the dead "Closed by Plan 3/5/9" refs (they point at the retired controller-panel numbering). Leave rows
   that **plan-6** closes marked `none-yet (post-beta)` — plan-6 flips them. Keep `route_policies` as a
   **reserved / reject-if-non-empty** note (D5b), do NOT flip to `semantic`.
5. **gofmt:** `gofmt -w` the 13 files; add a **CI gofmt gate** to `.github/workflows/ci.yml` over
   `./cmd ./internal` (fail on any drift). (Go not local — gate runs in CI.)
6. **controller-panel closure (Addition 1b):** assess `controller-panel-2026_06_08/` against the three
   `_completed` successor subjects; write `CLOSURE.md` (status DELIVERED-VIA-SUCCESSOR-SUBJECTS, noting the
   retracted mTLS+Postgres design); `git mv` the folder to `implementation_plans/_completed/`.
7. Commit per sub-step (`git commit -F`); push; PR.

## Tests produced
- CI gofmt gate — **perpetual** (`.github/workflows/ci.yml`). Guards: repo-wide gofmt cleanliness.
  Retirement: never.

## Definition of done
LICENSE+NOTICE present; CHANGELOG+RELEASING exist; README versions correct; wiki links redirected + both
wikis banner-scoped; validation.md honest (shipped rows flipped, dead refs gone, route_policies reserved-note,
plan-6 rows left `none-yet (post-beta)`); `gofmt -l` empty + CI gate live; controller-panel archived with
CLOSURE.md.

## Out of scope
Any validator CODE change (that is plan-6); the full wiki rewrite (GA); fixing the FileStore SPOF.

# plan-7 — docs + air-gap catalog fallback (design PR-5)

See `outline.md`. Milestone: **beta.1**. Prerequisites: plan-3 (reads the `artifacts.json` schema; can draft
alongside, finalize after plan-3). **D4 (omit when zero)** governs the air-gap default.

## Goal
Let air-gap / local mode supply the mimic pins (no `ControllerSettings` there), and make the mimic docs
honest about the GitHub-`.deb` fallback + trust chain.

## Reads from specs
`artifacts-signing`, `controller-stage-promote` (mimic prose: `docs/spec/artifacts/mimic.md`).

## Read first
- `cmd/compiler/main.go:78` (the air-gap render caller from plan-2); `internal/api/handler.go:159,:203`
  (local API render); `internal/render/render.go` (the `FetchSettings` plumbing from plan-2).
- `internal/bundlesig/bundlesig.go:59` (`LoadSigningFromEnv` — the env-opt-in idiom to mirror; call site `handler_bootstrap.go:133`).
- `docs/spec/artifacts/mimic.md` (`:25-29` not-bundled note — PRESERVE the GPL-2.0 non-obligation).

## Implementation steps
1. **Air-gap catalog input:** build `FetchSettings` from env/flags in the no-settings callers — a single
   `YAOG_ARTIFACT_CATALOG=/path/to/catalog.json` (same shape as `artifacts.json`) + `YAOG_GITHUB_PROXY` +
   `YAOG_MIMIC_VERSION`; `cmd/compiler` flags `--artifact-catalog`/`--gh-proxy`/`--mimic-version` layered
   over env. **Unset ⇒ zero `FetchSettings` ⇒ distro-only mimic + `artifacts.json` OMITTED** (D4 — air-gap
   byte-identity holds; verify plan-2 gate).
2. **Docs:** refresh `docs/spec/artifacts/mimic.md` — keep "not bundled" + GPL-2.0 note; document the
   pinned-GitHub-`.deb` fallback, the trust chain (pin ∈ `artifacts.json` ∈ `bundleFiles` ∈ signed
   checksums), and a populate-runbook for `ControllerSettings.MimicDebSHA256` (exact filenames + hashes from
   the mimic release — manual, D1). Forward-ref `agent-selfupdate.md` (created in plan-9).
3. Commit; push; PR.

## Tests produced
- Catalog-flag tests (env/flag → populated `FetchSettings`) — **subject-scoped**.
- Air-gap-omit assertion = the **perpetual** byte-identity gate from plan-2 (reused, not new).

## Definition of done
Air-gap compile with a catalog produces a valid signed bundle with mimic pins; WITHOUT a catalog the bundle
is byte-identical to today (D4); mimic.md reflects shipped behavior + a working runbook; docs link-check.

## Out of scope
`agent-selfupdate.md` (plan-9, where the swap exists); closing the bootstrap-TOFU hole (rc.2).

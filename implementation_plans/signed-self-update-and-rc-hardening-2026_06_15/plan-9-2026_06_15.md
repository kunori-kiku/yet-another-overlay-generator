# plan-9 — agent self-update mechanism + canary-then-fleet (design PR-4, the RISKY CORE)

See `outline.md`. Milestone: **beta.2**. Prerequisites: plan-4 (version reporting must exist so a crash-loop
is fleet-visible) + plan-3 (`artifacts.json` envelope). **D2 (canary-then-fleet)** locked — the empty-target
⇒ no-swap contract + the canary subset are the entire risk bound. This is the subject's highest-risk plan
(R1: brick-a-fleet).

## Goal
Let agents self-update their own binary, verified against the signed `artifacts.json` pin, rolled out
canary-subset-first then fleet, with a swap that cannot infinitely crash-loop a node.

## Reads from specs
`controller/agent-api`, `controller/keystone-trustlist`, `controller/stage-promote`, `controller/bootstrap`.

## Read first
- `internal/agent/verify.go:112-339` (DO NOT change signatures; ADD an `artifacts.json` presence+coverage
  guard mirroring `:176-181`), `state.go:34-58` (add `AgentVersionFloor`, `PendingUpdate`; `SaveState`
  temp-rename at `:94-98`), `cycle.go` (`RunControllerCycle` — hook point), `agent.go` (fail-safe),
  `cmd/agent/main.go:289` (`runControllerMode` — startup reconcile).
- `internal/api/handler_bootstrap.go:278-285` (arch map), `:295` (curl hardening), `:324-325`
  (`Restart=always RestartSec=5` — the loop to bound).
- `internal/controller/store.go:281-302` / `settings.go` / `handler_bootstrap.go` validation (add the
  target/canary settings); `artifacts.json` agent block (from plan-3, reserved).

## Implementation steps
1. **Settings (D2 canary):** `TargetAgentVersion`, `MinAgentVersion`, `AgentBinSHA256 map[string]Artifact`
   (`linux-<arch>`), and a **canary selector** (node-id list or a percentage) + a "promote canary → fleet"
   action; strict validation (D8). Fill the `artifacts.json` agent block from these (only for nodes in the
   active rollout set). Empty `TargetAgentVersion` ⇒ no agent block ⇒ no swap (the safety contract).
2. **`internal/agent/selfupdate.go`:** `maybeSelfUpdate(cfg, artifacts.agent)`:
   noop if `desired==running`; **anti-downgrade**: refuse `desired < AgentVersionFloor` and `desired <
   running`; build url `${GH_PROXY}${agent.release_url}/<asset-for-arch>`; download to
   `<dir(os.Executable())>/.yaog-agent.<desired>.partial` (same FS ⇒ atomic rename; cross-fs fallback
   copy+fsync+rename); **verify SHA vs signed `artifacts.json` pin** (refuse on mismatch); `exec(tmp,
   "version")` self-test (refuse if ≠ desired); write `State.PendingUpdate{from,to,attempts:0}` breadcrumb
   (crash-durable); `rename(target→target.bak); rename(tmp→target); syscall.Exec(target,...)`.
3. **Ordering (`min_version`):** if `running < agent.min_version` → self-update FORCED before apply (refuse
   to apply an incompatible bundle, report unhealthy); else self-update AFTER a successful apply.
4. **Startup reconcile** (`runControllerMode`, pre-loop): if `PendingUpdate` set — `BuildVersion==to` →
   health gate (one clean Poll+VerifyBundle) → promote (`AgentVersionFloor=to`, rm `.bak`, clear) or
   rollback (`rename(.bak→target)`, Exec back); `BuildVersion!=to` → `attempts++`; `>N` → abandon (stay
   `from`, cooldown, report unhealthy) — **bounds the `Restart=always` loop**.
5. **Legacy agents (Addition 4a):** define empty/absent reported version as **below-floor** so legacy
   agents self-update (not frozen out); test it.
6. **PRINCIPLES.md:** amend HIGH tier with the self-update custody invariant (also in outline Principles).
7. **Docs:** create `docs/spec/controller/agent-selfupdate.md` (protocol, floor, canary, min_version policy
   D3, recovery); refresh `wire-contract.md` for any new self-update wire.
8. **Canary UI:** panel surfaces canary set + per-node "update pending/applied/failed" (built on plan-4 badge).
9. Commit per sub-step; push; PR.

## Tests produced
- selfupdate decision table (noop / downgrade-refuse / floor-refuse / hash-mismatch-refuse / self-test-fail /
  happy) — **subject-scoped**.
- breadcrumb-reconcile (boot-as-`to`-healthy→promote, boot-as-`to`-unhealthy→rollback, boot-as-`from`→retry
  then abandon at cap) + cross-fs fallback — **subject-scoped**.
- **Perpetual** custody guard: an agent never execs a self-fetched binary unverified against the signed
  `artifacts.json` pin; never downgrades below floor (pins the new HIGH principle).

## Definition of done
A canary node self-updates + badge flips; a tampered hash is refused (keep-last-good); a crashing new binary
rolls back within the attempt cap (no infinite loop); downgrade refused; legacy empty-version updates;
documented manual recovery exists + tested; PRINCIPLES.md amended; `agent-selfupdate.md` created.

## Out of scope
Closing the bootstrap-TOFU hole (rc.2). Auto (non-canary) fleet rollout.

## Stop-loss
If bounding the `Restart=always` loop needs a systemd unit-file change (`StartLimitBurst`) rippling into the
bootstrap renderer → **STOP and draft plan-9.5**.

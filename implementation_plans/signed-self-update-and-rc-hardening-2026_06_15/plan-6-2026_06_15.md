# plan-6 — backend robustness & FULL input validation (Unit C + D5 full table)

See `outline.md`. Milestone: **beta.1**. Prerequisites: none (independent surface — validator, server.go,
Dockerfile, filestore.go). Runs parallel to 3/4/5. **D5 = full none-yet table** locked. Coordinates with
plan-1: plan-1 flips the ALREADY-SHIPPED `validation.md` rows; plan-6 flips only its newly-closed rows. The
`Dockerfile` is co-edited with plan-4 (ldflags) — pick an order (disjoint stages).

## Goal
Harden the backend for production: close the GENUINELY-missing validators (most `none-yet` rows are already
shipped on `main` — see the Read-first audit), abuse throttling, graceful shutdown, bounded audit log,
healthcheck, and a compile-time DoS bound.

## Reads from specs
`model-validation` (the coverage contract; deep prose `docs/spec/compiler/validation.md`), `controller-agent-api`, `controller-store`.

## Read first
- **READ-FIRST AUDIT (before writing ANY validator):** diff `docs/spec/compiler/validation.md` against the
  ACTUAL coverage in `internal/validator/schema.go` + `semantic.go`. Most `none-yet` rows are ALREADY shipped
  on `main`: `mtu` (`CodeNodeMTUOutOfRange`, schema.go:217), `router_id` MAC-48 (`CodeNodeRouterIDInvalid`,
  :231), `extra_prefixes[]` CIDR (:241-243), the four `ssh_*` via `sshFieldCharset` (:22,:249-255),
  `routing_mode` (:126-137), `route_policies` reject-if-non-empty (`code.go:160`). Those are NOT this plan's
  work (plan-1 flips their doc rows). The GENUINELY-missing set this plan closes: `endpoint_host` +
  `public_endpoints[].host` charset (today only a semantic WARNING, `semantic.go:726-751`, no schema gate)
  and `transit_cidr` (no IPv4-CIDR/host-pair validation).
- `internal/api/server.go:179-224` (listeners + timeouts already present; NO `Shutdown`/`signal.Notify`), `:208`
  (`/enroll` no-auth); serve goroutines in `cmd/server/main.go:178-179` (wire SIGTERM→Shutdown there);
  `internal/api/loginratelimit.go` (reuse for throttle).
- `internal/controller/filestore.go:48` (global mutex), `:903` (200ms poll), `:1561` `readAudit` → `:1574`
  `writeJSONAtomic` (full-file rewrite per append); `internal/controller/audit.go` (no cap).
- `Dockerfile` (no HEALTHCHECK — **also edited by plan-4 for ldflags; co-edit, disjoint stages**); compile
  entry for a node/edge count bound; `internal/compiler/compiler.go:14-18` (`AllocSchemaVersion` const) + `:114`
  (where it is stamped) — only version 1 exists today.

## Implementation steps
1. **Close the GENUINELY-missing validators (D5 full table — most rows already shipped, per the Read-first
   audit):** add schema charset/format gates for `endpoint_host` + `public_endpoints[].host` (injection-adjacent
   → root scripts; today only a semantic WARNING) and `transit_cidr` (IPv4-CIDR parse + enough-host-pairs
   bound). Each with a coded validator error + en/zh message. **Flip ONLY these newly-closed rows to
   `schema`/`semantic` HERE** (plan-1 already flipped the already-shipped rows). Do NOT re-implement or mint
   duplicate codes for the already-shipped validators. Validation rejects BEFORE render → does NOT move
   plan-2's byte-identity fixtures.
2. **`/enroll` throttle:** per-IP brute-force limiter (reuse `loginratelimit.go`).
3. **Graceful shutdown:** in `cmd/server/main.go:178-179` (the two serve goroutines), `signal.Notify(SIGTERM/SIGINT)`
   → `http.Server.Shutdown(ctx)` draining BOTH the operator and agent listeners (servers built in
   `internal/api/server.go`; timeouts already exist — do NOT relist those).
4. **Audit-log bound:** cap entries + stop the full-file RMW per append (`readAudit`→`writeJSONAtomic`);
   ring/rotate or append-mode.
5. **Docker HEALTHCHECK** → `/api/health`.
6. **Node/edge count bound:** reject oversized topologies at compile entry (distinct from the body-size cap).
7. **Schema-version forward-compat guard:** only `AllocSchemaVersion` v1 exists and absent/0 already restamps
   to 1, so there is no real *migration* to write. Instead make the load path REJECT a stored
   `AllocSchemaVersion > current` with a coded error (fail-closed against a future format); that rejection is
   the checkable gate. (A true upgrade branch lands when a v2 format exists.)
8. **FileStore SPOF:** **note-only** in code + `docs/spec/controller/persistence.md`; the mutex/200ms-poll fix
   is DEFERRED to rc.2/GA.
9. Commit per sub-step; push; PR.

## Tests produced
- Validator table tests (reject injection-adjacent hosts; accept valid) — **subject-scoped** (the
  charset/security ones may promote to **perpetual** if they pin a custody boundary).
- Graceful-shutdown drain test, `/enroll` throttle test, count-bound test — **subject-scoped**.

## Definition of done
Genuinely-missing validators (`endpoint_host` + `public_endpoints[].host` charset + `transit_cidr`) closed +
their rows flipped (already-shipped rows flipped by plan-1, so `validation.md` is fully honest); `/enroll`
throttles; SIGTERM drains both listeners; audit log bounded + no full rewrite; HEALTHCHECK green; count-bound
rejects oversized topologies; schema-version forward-compat guard rejects a future version; byte-identity
fixtures unmoved; `go test ./internal/...` green.

## Out of scope
The FileStore mutex/poll FIX (note-only); anything touching the bundle/render path.

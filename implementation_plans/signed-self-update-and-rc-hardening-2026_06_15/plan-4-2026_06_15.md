# plan-4 — agent version reporting + build-version injection + observability (design PR-3, observable half)

See `outline.md`. Milestone: **beta.1**. Prerequisites: plan-2 (struct) + plan-3 (shares
`ControllerSettings`/settings-validation block; lands after plan-3, rebase). **D7 (ldflags injection)** and
**D9 (release.yml agent-SHA emission)** locked.

## Goal
Make every node's running version *observable* (report → store → panel badge) and make `BuildVersion` real
via build-time ldflags — plus have `release.yml` publish per-arch agent binaries + SHA-256 so beta.2's
self-update has something to fetch and pin. **No swap logic, no target/canary settings** (those are beta.2).

## Reads from specs
`api/wire-contract`, `api/http-api`, `controller/agent-api`.

## Read first
- `cmd/agent/main.go:46` (subcommand switch), `:289` (`runControllerMode`).
- `internal/agent/controller_client.go:91-94` (`reportRequestWire` — no version field today).
- `internal/api/handler_controller.go:390-405` (`nodeJSON` — no version field); `internal/controller/store.go:74` (`Node` struct).
- `frontend/src/components/deploy/NodeRegistry.tsx` (status badge), `frontend/src/i18n/messages/{en,zh}.ts`.
- `.github/workflows/release.yml`, `Dockerfile`.

## Implementation steps
1. **Build version:** add `var BuildVersion = "dev"` + a `version` subcommand to `cmd/agent/main.go` (and
   `cmd/server`, `cmd/compiler` for parity). Inject via `-X main.BuildVersion=${tag}` ldflags in BOTH
   `release.yml` and `Dockerfile` (D7). Verify `yaog-agent version` prints the tag in a release build.
2. **release.yml agent artifacts (D9):** build + publish `yaog-agent-linux-amd64`/`-arm64` AND emit a
   `.sha256` per asset (so beta.2's `AgentBinSHA256` is populatable). Confirm the asset names match what
   beta.2 will fetch. (Closing the bootstrap-TOFU *hole* itself is deferred — document in `bootstrap.md`.)
3. **Report wire:** add `agent_version` to `reportRequestWire` (agent fills from `BuildVersion`) and to the
   server `reportRequestJSON`; persist onto `Node` (`store.go:74`) — store-interface touch, both impls.
4. **Panel:** add `agent_version` to `nodeJSON`; render a version line/badge in `NodeRegistry.tsx`; new i18n
   key in `en.ts`+`zh.ts` (**lockstep; plan-5 also touches these — plan-4 lands first**).
5. **Spec:** refresh `docs/spec/api/wire-contract.md` + `http-api.md` for the new `agent_version` field
   (Addition 3, was orphaned). Own the wire-field-parity test here.
6. Commit per sub-step; push; PR.

## Tests produced
- Report-wire parity test (`agent_version` present + threaded) — **subject-scoped**.
- Settings/round-trip unchanged (no new settings this plan).

## Definition of done
`yaog-agent version` prints the injected tag; a node's report carries `agent_version`; `nodeJSON` + panel
badge show running version; `release.yml` publishes per-arch agent binaries + `.sha256`; wire-contract.md
updated; en/zh lockstep.

## Out of scope
TargetAgentVersion/MinAgentVersion/AgentBinSHA256 SETTINGS + canary + the SWAP (all plan-9). Closing the
bootstrap-TOFU hole (rc.2).

## Stop-loss
If any change touches `cycle.go`/`agent.go` apply path → STOP (plan-4 is report-only).

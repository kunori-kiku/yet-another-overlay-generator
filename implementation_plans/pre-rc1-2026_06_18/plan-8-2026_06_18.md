# plan-8 — Remaining security + compiler-correctness fixes  (subject S1, milestone 1.8)

> Part of the pre-rc.1 program — see outline.md for mission, principles, and full sequencing.

## Prerequisites

- **plan-1 (1.1 hygiene)** must land first — plan-1 owns comment text / Englishization and several shared files (`peers.go`, `schema.go`, `roles.go`); plan-8's edits to those files rebase onto plan-1's hygiene pass per the spine "plan-1 before plan-2 + plan-8 (shared files)".
- **plan-2 (1.2 structure-redesign)** must land first — plan-2 (a) decomposes `internal/api/handler_controller.go` (~1984 lines) and `internal/controller/compile.go` (955 lines) into same-package sibling files, and (b) hoists the (unrelated) value types into `internal/model` (creating `internal/model/artifact.go`). **plan-2 does NOT move or own any shared allocation constant** — those are owned by plan-8, which single-sources them into a NEW `internal/allocconst` leaf package it creates itself (see Group D / coherence note; LOCKED OWNER DECISION 2026-06-18). plan-8's `handleOperatorCredentialPin` validator-call insertion and any other `handler_controller.go` edits rebase onto plan-2's file split (the moves are signature-neutral, so the rebase is mechanical).
- **plan-3 (1.3 compiler-extraction + frozen contract)** must land first for the ctx-plumbing foundation — plan-3 owns threading `context.Context` through `Compile`/`CompileSubgraph` as part of the frozen-contract extraction. **plan-8's S1 ctx work (the per-iteration `ctx.Err()` check in the allocator scan loop) consumes that plumbing** rather than introducing the signature change from scratch. If plan-3 has not yet threaded ctx to the allocator entry, plan-8 extends it the remaining hop (`AllocateIPs(ctx, …)` → `allocateFromCIDR(ctx, …)`); see Group A / cross-note.
- **Shipped (do NOT re-touch):** `v2.0.0-beta.8` already landed B1, F1, C1, S4, S5, S6 (reference only — see Background).
- **Feeds, not blocked-by:** the new `internal/allocconst` shared constants (Group D, owned + created by plan-8) must land BEFORE plan-4 (TS-compiler reimpl) mirrors them, and the C4 doc note tells plan-5 (conformance harness) to exclude `computeChecksum`. plan-8 only *feeds* plan-4/plan-5; it is not gated on them.
- **The ONLY 1.7-coupled item:** S3's cursor optimization (Group A, Phase 3) is MEDIUM + post-rc.1 and rides with **plan-7 (1.7 backend-shrink)**'s air-gap route removal; it is the single piece of this plan that defers.

## Goal

Close every **confirmed** security and compiler-correctness finding from the pre-rc.1 investigation that was **not** already shipped in `v2.0.0-beta.8`, and record an explicit FIX-vs-DOCUMENT decision for each of the four DECISION-REQUIRED acceptances (B2, B3, S9, S10). This is SUBJECT 1's residual security/correctness sweep between the headline beta.8 blockers (landed) and the rc.1 tag: compile-time DoS hardening (S1 cap **and** `context.Context` together — both now, as the named rc.1 blocker; S2 unbounded domains/reserved-ranges; S3 quadratic gap-fill, the one post-rc.1 item), SSRF completeness (S7 IPv4-compatible IPv6, S8 dial-error DNS oracle, S11 OCI `192.0.0.192`), operator-input validation (bootstrap RPID/Origin arg-injection FIX; TOFU-no-pin-no-keystone MITM DOCUMENT), compiler correctness (C2 re-enable collision auto-heal, C3 `HasPublicIP` endpoint omission, C4 `%v` checksum doc note, shared-constant single-sourcing into a NEW `internal/allocconst` leaf package plan-8 owns), and the four decisions (B2 FileStore fsync FIX, B3 passkey-login Origin FIX, S9 per-username login lockout DOCUMENT, S10 CSRF double-submit DOCUMENT). Every fix ships behind a repro-first failing test; SUBJECT 4's final audit re-checks the whole surface afterward.

## Reads from specs

docs/spec/rc1/investigation-report.md (§2 Security, §6 Compiler, §8 Completeness, §9 Prioritized split), docs/spec/rc1/investigation-findings.json (`.security.confirmed_full`, `.compiler.correctness_findings`, `completeness`), docs/spec/compiler/allocation-stability.md (invariants I2/I3/I9 the S3/C2/C3 equivalence pinning protects), docs/spec/security/security.md (target for the DOCUMENT decisions), docs/spec/artifacts/babel.md (Link cost resolution — `backupDefaultLinkCost` provenance), docs/spec/roles/roles.md (D49 router/gateway accept-inbound semantics for C3), docs/spec/data-model (node-model spec the C3 derive-up note lands in)

## Read first

Read these CODE files at these lines, in order, before editing:

1. `internal/allocator/ip.go:22-85` (`AllocateIPs` + the per-node loop at `:62-82`) then `:88-174` (`allocateFromCIDR`: `totalHosts` at `:125`, scan loop `:143-171`, the per-candidate `reservedNets` `Contains` walk `:160-165`, the `hostBits >= 32` safety net `:121`) — S1 cap + ctx landing site.
2. `internal/validator/schema.go:65-103` (the `maxTopologyNodes`/`maxTopologyEdges` const block `:65-68`, `topologyExceedsBounds` `:76-80`, the `ValidateSchema` short-circuit reporter `:89-103`) then `:158-163` (the `< /8` CIDR-too-large guard) — S2 mirrors this exact pattern.
3. `internal/compiler/peers.go:21` (`defaultTransitCIDR`), `:137` (`backupDefaultLinkCost`), `:780-786` (the `fromNode.Capabilities.HasPublicIP && len(fromNode.PublicEndpoints) > 0` reverse-endpoint gate), the gap-fill sites `:938`/`:956`/`:970`, and the reserve-first + linkKey-sorted invariant at `:484` — Group A S3 + Group D constants + C3.
4. `internal/validator/semantic.go:499` (`defaultTransitCIDR`), `:636` (`minPinnedPort`), `:798` (`backupDefaultLinkCost`) — duplicated-constant sites.
5. `internal/renderer/script.go:805` (`defaultTransitCIDR`, used at `:932,941,943,960`) — third dup site; note `renderer` does NOT import `linkid` (so do NOT home the consts there), but `renderer` DOES already import `internal/model` (`script.go:7`) and will newly import the leaf `internal/allocconst` — verify the import direction (`compiler`/`validator`/`renderer` → `allocconst`, a pure leaf importing nothing; no back-edge, no cycle).
6. `internal/compiler/roles.go:108-134` (`InferCapabilitiesFromRole`; reads `node.Capabilities.HasPublicIP` directly in the router `:116` and gateway `:124` `CanAcceptInbound` branches, NOT the local `caps` var at `:109`) — C3 cascade.
7. `internal/compiler/compiler.go:97-120` (`Compile` → `AllocateIPs` at `:118`) then `:234-238` (`computeChecksum` = `sha256(fmt.Sprintf("%v", topo))[:16]`) — S1 ctx ripple + C4.
8. `internal/api/handler.go:152,203,273` (the three `h.compiler.Compile(topo, keys)` callers; `r.Context()` available at each) — S1 ctx source.
9. `internal/controller/compile.go:138` (`CompileSubgraph` — currently NO ctx param) and `:292` (`CompileAndStage` — already `ctx context.Context`) — controller compile arm for S1 ctx (note: ctx-plumbing through these is plan-3's; plan-8 reads the post-plan-3 signatures).
10. `internal/api/release_pins.go:60-111` (`isPublicUnicastIP` `:70-91`, the `To4()` unwrap `:78`, `embeddedIPv4` 6to4/NAT64 `:99-111`, `blockPrivateAddr` reject string `:60-61`) and the `fetchSidecar` error path (the `.With("detail", err.Error())` that leaks the resolved IP) — Group B SSRF.
11. `internal/api/handler_bootstrap.go:224` (the `ContainsAny(s, " \t\r\n\v\f")` whitespace reject), `:259-270` (`validateMimicCatalog` with the `ContainsAny(…, "$\`;|&<>(){}[]'\"\\*? ")` reject at `:270`), `:349-350` (`shQuote` at assignment), `:439-446` (the `OP_FLAGS` accumulator), `:466` (`ExecStart` unquoted `${OP_FLAGS}`), `:487-488` (the `# shellcheck disable=SC2086` + the `--once` unquoted `$OP_FLAGS`) — bootstrap arg-injection.
12. `internal/api/handler_controller.go:1679-1762` (`handleOperatorCredentialPin`: builds `newCred` at `:1707`, `RPID:req.RPID` `:1711`, `Origin:req.Origin` `:1712`) — bootstrap validate-at-pin call site (rebase onto plan-2's split).
13. `internal/api/handler_passkey.go:228-244` (the RPID required-check `:232-234`, the `LoginCredential` pin `:240-244` with `Origin:req.Origin` `:242`, no Origin required-check) and `:363-368` (the decoy nonexistent-user uniform-401 with `RPID:""` — NOT a pin site) — B3.
14. `internal/controller/trustlist/webauthn.go:169-171` (the `if pin.Origin != ""` advisory origin gate) — B3 becomes authoritative for login pins without crypto change; same verifier used by the node keystone path (`internal/agent/verify.go`) where empty Origin is intentional.
15. `internal/controller/filestore.go:217-231` (`writeJSONAtomic`: `os.WriteFile(tmp)+os.Rename`, no `Sync`) and `:1968-1990` (`AppendAudit`: `f.Write`+`f.Close`, no `f.Sync`; the "durably appended" comment in the rotate block) — B2.
16. `internal/api/loginratelimit.go:85` (`registerAttempt` reserves on `user:<username>` AND `ip:` keys) and `internal/api/cookie_session.go:95-97` (`csrfValid` double-submit, no session binding) — S9 / S10 DOCUMENT.
17. `internal/regression/keystone_regression_test.go` — must stay green (whole-suite gate).

## Implementation steps

> Note on line numbers: the source-plan anchors were re-verified against this branch. A few drifted (`CompileSubgraph` is now `compile.go:138` and takes NO ctx; `CompileAndStage` is `compile.go:292` and already takes ctx; the `AppendAudit` close block is `filestore.go:1968-1990`; the bootstrap whitespace reject is `handler_bootstrap.go:224`). Use the verified values above. plan-1/plan-2/plan-3 will shift them again on rebase — re-grep the symbol, do not trust a stale `:NNN`.

Each finding is an **independently reviewable PR** per the review-each-PR-before-merge discipline. Phase 1 (repros) lands first; Phases 2/4/5/6/7 are the rc.1-blocking fix set and may proceed in parallel; Phase 3 (S3) follows plan-7.

### Phase 1 — Repro harness first (failing tests before any fix)

Reproduce each open bug in an EXISTING test file, or one explicitly marked **new** (verified: `internal/validator/schema_test.go` and `internal/compiler/peers_test.go` do NOT exist — do not reference them; `internal/compiler/allocation_pins_test.go` does NOT exist — the compiler-side C2 regression goes in a new `internal/compiler/reenable_heal_test.go`):

1.1 S1/S2 DoS-bound tests: S1 in `internal/allocator/ip_test.go` (oversized CIDR × nodes → expect a fast coded reject, NOT a timeout); S2 in `internal/validator/plan6_safety_test.go` (over-cap domain / reserved-range counts — this file already houses the nodes/edges DoS-bound tests).
1.2 S3 cursor-equivalence test in `internal/compiler/allocation_stability_test.go` + `internal/compiler/transit_pools_test.go` (many same-pair `backup` edges; assert allocations byte-identical vs the current restart-from-0 values).
1.3 SSRF unit tests in `internal/api/release_pins_test.go`: `isPublicUnicastIP(::127.0.0.1)`, `(::169.254.169.254)`, `(192.0.0.192)` all false; `fetchSidecar` error-detail redaction (body carries NO resolved IP).
1.4 C2 re-enable regression in **new** `internal/compiler/reenable_heal_test.go`: disable A-B → add A-C → re-enable A-B; assert a clean compile, A-B gets a fresh non-colliding pin, A-C unchanged. Validator-side counterpart in the existing `internal/validator/allocation_pins_test.go`.
1.5 C3 tests in the existing `internal/compiler/peers_endpoint_test.go` (`has_public_ip=false` + endpoint present → non-empty reverse Endpoint at `peers.go:783-785`) AND `internal/compiler/roles_test.go` (router/gateway with endpoint + `has_public_ip=false` → `CanAcceptInbound` flips).

Commit Phase 1 with the repros RED (or skipped-with-reason if a RED test would break CI), then green them in the corresponding phase below.

### Phase 2 — Group A: S1 (FULL: cap + ctx) + S2 (all NOW, rc.1-blocking)

2.1 **S2:** add `maxTopologyDomains` and `maxReservedRangesPerDomain` to the `schema.go:65-68` const block; extend `topologyExceedsBounds` (`:76-80`) and the `ValidateSchema` short-circuit reporter (`:89-103`) with two new coded errors, mirroring the existing nodes/edges pattern exactly (count → `result.AddError(field, CodeTopologyTooMany…, P{"count",…}, P{"max",…})`). Add the two codes to the apierr table.
2.2 **S1 cap (blocking):** before the scan loop, bound `nodes × min(totalHosts, ceiling)` at the `AllocateIPs` entry (`ip.go:22`, loop `:62`) and/or in `allocateFromCIDR` (`ip.go:88`; `totalHosts` is already at `:125`), returning a new coded error `CodeOverlayScanBudgetExceeded`. Pick the ceiling generous — it only needs to stop "millions", matching the existing nodes/edges bound philosophy (`schema.go:60-64`); the cap value is **owner-facing** (see Definition of done / owner flag R6).
2.3 **S1 ctx (blocking):** thread `context.Context` into `AllocateIPs(ctx, topo)` → `allocateFromCIDR(ctx, …)` and check `ctx.Err()` every K iterations of the scan loop (`ip.go:143`). The request `ctx` already exists at every HTTP boundary: pass `r.Context()` from the three `handler.go` callers (`:152/203/273`) through `compiler.Compile(ctx, …)` (`compiler.go:97/118`); the controller arm is `CompileSubgraph` (`compile.go:138`) / `CompileAndStage` (`compile.go:292`, already `ctx`-taking). **Cross-note:** plan-3 owns the `Compile`/`CompileSubgraph` ctx-plumbing as part of the frozen-contract extraction — if plan-3 has already threaded ctx down to the allocator entry, plan-8 only adds the `ctx.Err()` check inside the loop; otherwise plan-8 extends the remaining hop. Either way it is mechanical; `allocation_stability_test.go` + the green suite pin behavior.

### Phase 3 — Group A: S3 cursor (the ONLY 1.7-coupled item — sequence with plan-7)

3.1 In `peers.go`: (1) parse the transit CIDR ONCE per pool (cache `*net.IPNet` keyed by CIDR string) instead of re-`ParseCIDR` per `allocateTransitPair`; (2) replace restart-from-0 in `gapFillTransitPair` (`:938`), `gapFillLinkLocalPair` (`:956`), and `lowestFreePort` (`:970`) with a per-pool monotone free cursor — sound ONLY because reserve-first + linkKey-sorted iteration (`peers.go:484`) means no later-sorted link needs an earlier-freed slot; (3) add `ctx.Err()` checks in the loops. **Prove byte-identity** against `allocation_stability_test.go` + the Phase 1.2 equivalence test BEFORE merging. If equivalence cannot be proven, ship parse-once-cache + `ctx` only and leave restart-from-0 (see risk R2). This phase rides with plan-7's air-gap route removal (which also touches the same compile chain).

### Phase 4 — Group B: SSRF completeness (NOW, all in `release_pins.go`)

4.1 **S7:** extend `embeddedIPv4` (`:99`) or `isPublicUnicastIP` (`:78`) to decode the IPv4-compatible form `::a.b.c.d`: when `ip.To16()` has a zero high-96 and a non-zero low-32 that is a valid non-trivial IPv4, recurse `isPublicUnicastIP(embedded)`. Guard against `::` (unspecified — already caught at `:72`) and tiny values.
4.2 **S8:** in `fetchSidecar`, collapse all transport/dial/read failures to a fixed generic detail (e.g. `"fetch failed"`); keep the inner `err` ONLY in the server log, never in `.With("detail", …)`. The status-code branch may keep `"HTTP <code>"` (no IP leak).
4.3 **S11:** add `192.0.0.0/24` (broader; or `/32` minimal) to the deny set in `isPublicUnicastIP` (`:78-81`) alongside the CGNAT check.

### Phase 5 — Group C: input validation + MITM doc (NOW)

5.1 **bootstrap arg-injection (FIX):** add a `validateOperatorCredentialBinding` helper next to `validateMimicCatalog` (`handler_bootstrap.go:259`) that rejects RPID/Origin containing the same shell-dangerous byte class (`:270`) **plus whitespace** (the injection here is word-splitting, so whitespace is the load-bearing rejection — reuse the `handler_bootstrap.go:224` whitespace class). Call it in `handleOperatorCredentialPin` (`handler_controller.go:1679`, before building `newCred` at `:1707`) — **this call site rebases onto plan-2's `handler_controller.go` split** (the function may have moved to a sibling file; re-grep `handleOperatorCredentialPin`). **Do NOT double-quote `${OP_FLAGS}`** — it is a multi-flag accumulator (`handler_bootstrap.go:444-446`) that REQUIRES word-splitting to expand into separate argv entries; the `--once` path carries an explicit `# shellcheck disable=SC2086` at `:487` precisely because the unquoted `$OP_FLAGS` at `:466/:488` is intentional. Quoting it would pass the whole string as one argument and break enrollment. Once RPID/Origin cannot contain whitespace or metacharacters the unquoted expansion is safe **by construction** — state this in a code comment near `:445-446`/`:487` so a future reader does not "fix" the SC2086-disabled line. (Validate-at-pin is the COMPLETE fix, mirroring the mimic-catalog discipline.)
5.2 **TOFU-MITM (DOCUMENT):** correctly defended by the keystone; the residual is a back-compat default the code comments already flag. Add an **rc.1 release-notes hard requirement** ("controller mode in production REQUIRES keystone OR TLS-or-pinned-pubkey; plain-HTTP + keystone-OFF + no-pubkey is dev-only") rather than a behavior change that could break existing keystone-OFF dev deployments. Add a single startup log warning when `EnableController && keystone OFF && no TLS hint` is detected (near the `EnableController` wiring in `server.go`). Refuse-TOFU in code is rc.2/GA bootstrap-TOFU work (already deferred).

### Phase 6 — Group D: compiler correctness (constants feed plan-4 — do early)

6.1 **Constants (FIX) — single-source into a NEW `internal/allocconst` leaf package, OWNED BY plan-8 (LOCKED OWNER DECISION 2026-06-18; NOT `internal/model`).** **COHERENCE:** plan-2's `internal/model` work is only the (unrelated) Artifact value-type hoist (`internal/model/artifact.go`); plan-2 does NOT move or own any shared allocation constant. plan-8 therefore CREATES a new pure-leaf package `internal/allocconst` (a single `internal/allocconst/allocconst.go` with `package allocconst`, importing nothing) holding `DefaultTransitCIDR` (`"10.10.0.0/24"`), `BackupDefaultLinkCost` (`384`), and `MinPinnedPort` (`1024`) as exported consts, then deletes the three private duplicates and references the new package from `compiler/peers.go:21,137`, `validator/semantic.go:499,636,798`, and `renderer/script.go:805` (+ the uses at `script.go:932,941,943,960`). **Do NOT home them in `internal/model`** (keeps the alloc constants in a dedicated single-purpose leaf, decoupled from the model value-type churn plan-2 owns) and **do NOT hoist into `internal/linkid`** (`renderer` does NOT import `linkid`, so referencing the consts from `linkid` would add a spurious `renderer→linkid` edge). **Import-direction (verified): `compiler`, `validator`, and `renderer` may all import `internal/allocconst` because it is a pure leaf importing nothing back — no cycle (`allocconst` depends on no internal package; the three already import `internal/model` for unrelated reasons, and `model` imports none of them, so the new package introduces exactly one fresh downward edge each).** This is the single source plan-4's TS port mirrors. Record the `deploy.go:310,370,482,568` hardcoded-`10.10.0.0/24` SNAT-literal mismatch (ignores a custom `transit_cidr`) as a SEPARATE follow-up for SUBJECT 3's pitfall hunt — a real behavior bug, NOT fixed here.
6.2 **C3 (FIX, explicit cascade):** in `InferCapabilitiesFromRole` (`roles.go:108`), derive a local `hasPub := node.Capabilities.HasPublicIP || len(node.PublicEndpoints) > 0`, set `caps.HasPublicIP = hasPub` (normalize UP only — never strip an explicit `true`), and use `hasPub` (NOT `node.Capabilities.HasPublicIP`) in the router (`:116`) and gateway (`:124`) `CanAcceptInbound` branches. The cascade is intentional and correct (D49): a node with a real public endpoint SHOULD accept inbound. This fixes the reverse-Endpoint omission at `peers.go:783-785` AND flips `CanAcceptInbound` for endpoint-bearing router/gateway nodes — pin BOTH behaviors with the Phase 1.5 tests.
6.3 **C2 (FIX, auto-heal-on-reenable):** `model.Edge` has no age/timestamp field (`topology.go` carries only `IsEnabled` + slice order), so "keep the longer-lived edge" is **uncodeable** — do NOT use it. Instead, extend `HealCollidingPins` in `normalize/pins.go` so that on a cross-link collision between two ENABLED edges it strips the pins of the edge with NO reserved backing (the re-enabled one — the discriminator is "which edge's pin value is reproduced by reserve-first allocation of the OTHER edge"; the colliding pin the incumbent legitimately owns wins, the other is stripped), NOT merely the later one in array order. Pass 1's reserve-first + linkKey-sorted gap-fill (`peers.go:484`) then re-allocates the stripped edge into a free slot — because the incumbent (e.g. A-C) is reserved first, no collision is possible. Spell out in the implementation which discriminator is used. Keep the loud `CodePin*DuplicateCrossLink` (`semantic.go`) as the safety net for any path the heal misses. (Matches report §9:155.)
6.4 **C4 (DOCUMENT-in-code):** add a doc comment on `computeChecksum` (`compiler.go:234`) marking it display-only, non-canonical, NOT the bundle digest, and explicitly out-of-scope for plan-5's Go↔TS conformance harness (a TS port cannot reproduce `fmt.Sprintf("%v", topo)`). No behavior change.

### Phase 7 — Group E: decisions (firm recommendations + owner flags)

7.1 **B2 → FIX (not owner-facing):** crash-consistency hardening of the identity/credential/trust-list store; distinct from the deferred host-loss SPOF. Three mechanical changes: (1) in `writeJSONAtomic` (`filestore.go:217-231`) replace `os.WriteFile(tmp, …)` with `os.OpenFile(tmp, O_WRONLY|O_CREATE|O_TRUNC, 0600)` + `Write` + `Sync` + `Close`; (2) fsync the parent dir after `os.Rename` (`os.Open(dir)+Sync()+Close()`); (3) in `AppendAudit` add `f.Sync()` before `f.Close()` (`filestore.go:~1976`), making the "durably appended" comment in the rotate block accurate. Test in a **new** `internal/controller/filestore_durability_test.go`.
7.2 **B3 → FIX (mechanism not owner-facing; legacy-pin cutover IS owner-facing):** add a non-empty `Origin` required-check at `handler_passkey.go:~232`, mirroring the existing RPID check (`CodeReqFieldRequired`, field=`origin`), so login pins always carry an Origin. Then `webauthn.go:170`'s `if pin.Origin != ""` is always-true for login pins and becomes authoritative **without touching the shared `trustlist.VerifyAssertion` crypto** (also used by the node keystone path, where empty Origin is intentional). Do NOT touch the decoy at `handler_passkey.go:367` (`RPID:""` — the nonexistent-user uniform-401, not a pin site). Gate enforcement to NEW pins + emit a startup warning for legacy login pins saved without an Origin; **confirm with the owner before enforcing on existing pins** (owner flag R5).
7.3 **S9 + S10 + TOFU-MITM (DOCUMENT):** write a security-assumptions section under `docs/spec/security/security.md` + the rc.1 release notes. S9: per-username lockout is inherent to any account-lockout; the break-glass `YAOG_CONTROLLER_OPERATOR_TOKEN` bypasses `/login` entirely — record as accepted; optional stretch softens the username dimension to a delay while keeping the IP dimension hard (`loginratelimit.go:85`). S10: double-submit CSRF is safe for the intended single-controller exact-origin-allowlist + TLS-proxy deployment — record the threat boundary (TLS proxy + no untrusted sibling subdomains + `YAOG_SECURE_COOKIE=true`, `cookie_session.go:97`); server-side csrf↔session binding is a defense-in-depth stretch, not rc.1.

### Phase 8 — Verify + roll

Run, from the repo root:

```bash
go build ./... && go vet ./... && go test ./... -race
go test ./internal/regression/ -run TestRegression   # keystone suite must stay green
gofmt -l internal/ | tee /dev/stderr | (! grep .)   # empty == clean
```

(MEMORY: verify locally; CI is the authoritative gate. Module access via the persisted `GOPROXY`/`GOSUMDB=off`.)

Commit each phase as its own PR. Suggested final commit message for the fix-release roll-up (HEREDOC-safe):

```bash
git commit -F - <<'EOF'
fix(security,compiler): close residual pre-rc.1 findings (milestone 1.8)

DoS: S1 allocator cap (CodeOverlayScanBudgetExceeded) + ctx-cancel in the
scan loop; S2 domain/reserved-range count bounds; S3 parse-once + monotone
free cursor (byte-identical, rides plan-7).
SSRF: S7 IPv4-compatible-IPv6 unwrap, S8 generic dial-error detail (no IP
leak), S11 deny 192.0.0.0/24 (OCI metadata).
Input: bootstrap RPID/Origin validate-at-pin (no whitespace/metachars;
OP_FLAGS stays unquoted by construction). TOFU-MITM documented.
Compiler: C2 re-enable auto-heal in normalize/pins.go; C3 HasPublicIP
derive-up cascade (reverse Endpoint + CanAcceptInbound); C4 checksum
doc-only note; shared constants single-sourced into new internal/allocconst.
Decisions: B2 filestore fsync (writeJSONAtomic + AppendAudit) FIX; B3
passkey-login Origin required-check FIX; S9/S10 DOCUMENT.

Repro-first tests for every finding. No beta.8-shipped code re-touched.
EOF
git push -u origin HEAD
```

(Branch first if on `main`. Cut the fix release only AFTER SUBJECT 4 re-audits.)

## Tests produced by this plan

- **`internal/allocator/ip_test.go`** (extend) — **Lifetime:** perpetual. **Guards:** S1 — an oversized CIDR × node count returns a fast coded reject (`CodeOverlayScanBudgetExceeded`), never a timeout; the scan loop honors `ctx` cancel. **Retirement trigger:** the allocator scan is removed entirely (e.g. a different allocation algorithm with no enumerate loop). **Retirement destination:** tests/legacy/allocator/.
- **`internal/validator/plan6_safety_test.go`** (extend) — **Lifetime:** perpetual. **Guards:** S2 — over-cap domain / reserved-range counts return a coded reject before the expensive passes run (the DoS-bound invariant, same family as the existing nodes/edges bounds). **Retirement trigger:** the `topologyExceedsBounds` root guard is removed or replaced. **Retirement destination:** tests/legacy/validator/.
- **`internal/compiler/allocation_stability_test.go` + `internal/compiler/transit_pools_test.go`** (extend) — **Lifetime:** perpetual. **Guards:** S3 — many same-pair `backup` edges produce byte-identical allocations under the parse-once + monotone-cursor path vs restart-from-0 (the I2/I9 order-independence invariant). **Retirement trigger:** the allocation-stability contract in docs/spec/compiler/allocation-stability.md is intentionally changed. **Retirement destination:** tests/legacy/compiler/.
- **`internal/api/release_pins_test.go`** (extend) — **Lifetime:** perpetual. **Guards:** SSRF — `isPublicUnicastIP(::127.0.0.1)`, `(::169.254.169.254)`, `(192.0.0.192)` all false; `fetchSidecar` error body carries NO resolved internal IP. **Retirement trigger:** the egress-guard predicate is removed (e.g. the assisted release fetch is deleted with plan-7's route removal). **Retirement destination:** tests/legacy/api/.
- **`internal/compiler/reenable_heal_test.go`** (NEW) — **Lifetime:** subject-scoped (S1). **Guards:** C2 — disable A-B → add A-C → re-enable A-B compiles cleanly; A-B gets a fresh non-colliding pin, A-C unchanged. **Retirement trigger:** the C2 auto-heal is folded into a broader pin-heal regression file, or `HealCollidingPins`'s cross-link rule is redesigned. **Retirement destination:** tests/legacy/compiler/.
- **`internal/validator/allocation_pins_test.go`** (extend) — **Lifetime:** subject-scoped (S1). **Guards:** C2 validator-side — the loud `CodePin*DuplicateCrossLink` safety net still fires for any path the heal misses. **Retirement trigger:** the duplicate-cross-link coded error is removed. **Retirement destination:** tests/legacy/validator/.
- **`internal/compiler/peers_endpoint_test.go` + `internal/compiler/roles_test.go`** (extend) — **Lifetime:** perpetual. **Guards:** C3 — `has_public_ip=false` + a configured public endpoint → non-empty reverse Endpoint AND (router/gateway) `CanAcceptInbound=true` (the derive-up cascade; pins both behaviors so the cascade is intentional, not accidental). **Retirement trigger:** the role/capability inference model in docs/spec/roles/roles.md is changed. **Retirement destination:** tests/legacy/compiler/.
- **`internal/controller/filestore_durability_test.go`** (NEW) — **Lifetime:** subject-scoped (S1). **Guards:** B2 — `writeJSONAtomic` (OpenFile+Sync+parent-dir fsync) and `AppendAudit` (`f.Sync()` before close) have no error-path regression on an injected temp dir (durability itself is verified by code-review + crash-reasoning; this is the no-regression net). **Retirement trigger:** FileStore is replaced by a different persistence backend (the deferred SPOF re-architecture). **Retirement destination:** tests/legacy/controller/.
- **Constants single-sourcing round-trip** (in an existing compiler/validator test, e.g. `allocation_stability_test.go`) — **Lifetime:** subject-scoped (S1). **Guards:** validator and compiler agree on a boundary-value pin (validate→compile round-trip) now that the constants are single-sourced in the new `internal/allocconst` leaf package — a compile-time single-definition guarantee replaces the comment-only sync. **Retirement trigger:** the shared constants are inlined back or moved out of `internal/allocconst` by a later structural plan. **Retirement destination:** tests/legacy/compiler/.

## Definition of done

- [ ] **S1:** an adversarial compile body (max nodes + `/8` CIDR + low reserved `/9`) returns a FAST coded error (`CodeOverlayScanBudgetExceeded`), and a long scan is ABORTABLE on request cancel (`ctx.Err()`) — both proven by test.
- [ ] **S2:** thousands of domains / reserved-ranges return a coded reject before the expensive passes run; two new coded errors in the apierr table.
- [ ] **S3:** thousands of same-pair `/12` backups compile fast with BYTE-IDENTICAL allocations (or, if equivalence unproven, parse-once-cache + ctx only, restart-from-0 retained — risk R2). Rides with plan-7.
- [ ] **S7:** `isPublicUnicastIP(::127.0.0.1)` and `(::169.254.169.254)` return false. **S11:** `(192.0.0.192)` returns false.
- [ ] **S8:** a guard-rejected `fetchSidecar` returns an error body with NO resolved internal IP; the IP remains in the server log only.
- [ ] **bootstrap:** pinning RPID/Origin with a shell-dangerous byte OR whitespace is rejected at pin time with a coded error; `${OP_FLAGS}` is left UNQUOTED with a comment explaining it is safe by construction.
- [ ] **C2:** disable→add→re-enable compiles cleanly; the re-enabled edge gets a fresh non-colliding pin, the incumbent edge is unchanged (regression green); the loud duplicate-cross-link error remains as the safety net.
- [ ] **C3:** a node with `has_public_ip=false` + a configured public endpoint produces a non-empty reverse Endpoint AND (for router/gateway) `CanAcceptInbound=true`; normalize UP only (an explicit `true` is never stripped).
- [ ] **C4:** `computeChecksum` carries a doc comment marking it display-only / non-canonical / out-of-conformance for plan-5.
- [ ] **Constants:** `DefaultTransitCIDR` / `BackupDefaultLinkCost` / `MinPinnedPort` have exactly ONE definition each, in the NEW `internal/allocconst` leaf package plan-8 created and owns (NOT `internal/model`), referenced everywhere; `renderer` does NOT import `linkid` for them; the `compiler`/`validator`/`renderer` → `allocconst` import direction is acyclic (`allocconst` imports nothing back).
- [ ] **B2:** `writeJSONAtomic` (OpenFile+Sync+parent-dir fsync) and `AppendAudit` (`f.Sync()` before close) are durable; the "durably appended" comment is accurate.
- [ ] **B3:** passkey-login pins require non-empty Origin (NEW pins) with the login origin check authoritative; shared `VerifyAssertion` crypto untouched; legacy pins warn (NOT enforced until owner confirms — R5).
- [ ] **S9 + S10 + TOFU-MITM:** recorded DOCUMENT decisions live in `docs/spec/security/security.md` + rc.1 release notes; the controller emits a startup warning for the keystone-OFF + no-TLS dev posture.
- [ ] **Whole-suite gate:** `go build ./... && go vet ./... && go test ./... -race` green; the keystone regression suite (`internal/regression/keystone_regression_test.go`) green; `gofmt -l` empty; NO beta.8-shipped code (B1/F1/C1/S4/S5/S6) re-touched.
- [ ] **Owner flags surfaced** (see `owner_flags`): R6 the S1 cap ceiling value; R5 the B3 legacy-pin cutover; and (only if the owner still wants it) deferring the S1-ctx half to ride with plan-7 — NOT baked in by default.

## Out of scope for this plan

- Anything shipped in `v2.0.0-beta.8` (B1 panic recovery, F1 keystone-sign-401, C1 babeld byte-stability, S4 revoked-resurrection, S5 enrollment-token purge, S6 token-TTL cap) — reference only, do NOT re-touch.
- The TypeScript local-mode compiler rewrite (plan-4) + the Go↔TS conformance harness (plan-5) — this plan only *feeds* them (the new `internal/allocconst` single-sourced constants, the C4 exclusion note) and *benefits* from plan-7's route removal. The harness is built FIRST and isolated from the active PR stream per the locked scope.
- The `internal/model` file layout (the Artifact value-type hoist / `internal/model/artifact.go`) — owned by plan-2; plan-8 does NOT touch it. The shared allocation constants are NOT part of that decision: per the LOCKED OWNER DECISION (2026-06-18) plan-8 creates and owns a NEW `internal/allocconst` leaf package for them — plan-2 defers ALL shared allocation constants to plan-8/allocconst. plan-8 does NOT home the consts in `internal/model` and does NOT hoist into `internal/linkid`.
- The `Compile`/`CompileSubgraph` ctx-plumbing SIGNATURE — owned by plan-3 (frozen-contract extraction); plan-8 only adds the in-loop `ctx.Err()` check on top.
- **B4** export-ZIP in-memory amplification — same anonymous-local-mode DoS class as S1–S3; folded into plan-7's route-removal disposition, not re-fixed here (noted so SUBJECT 4 sees it tracked).
- The `deploy.go:310,370,482,568` hardcoded-`10.10.0.0/24` SNAT-literal mismatch (ignores a custom `transit_cidr`) — a real behavior bug, recorded as a follow-up for SUBJECT 3's pitfall hunt (plan-16), NOT fixed here.
- FileStore host-loss SPOF, bootstrap-TOFU re-architecture / refuse-TOFU-in-code, KMS / OIDC / multi-tenant — deferred to rc.2/GA (B2 fixes ONLY per-record crash-consistency, not the SPOF; TOFU-MITM is DOCUMENTed, not refused-in-code).
- The `internal/render` vs `internal/renderer` rename, CJK-comment Englishization (plan-1 owns comment text), frontend test runner, `cmd/*` coverage — all post-rc.1 per report §3c/§4/§7.
- Server-side csrf↔session binding (S10 hardening) and the soft-username-lockout (S9 stretch) — defense-in-depth, not rc.1; recorded as DOCUMENT.

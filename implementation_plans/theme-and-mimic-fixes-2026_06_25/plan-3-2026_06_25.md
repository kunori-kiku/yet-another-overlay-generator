# plan-3 — Release the theme+mimic fixes beta

**Subject:** theme-and-mimic-fixes-2026_06_25 · **Track:** release · **Depends on:** plan-1, plan-2
(both merged + green).

## Goal

Ship the two fixes as one beta promoted to GitHub Latest, and deliver the owner real-host mimic smoke
runbook that gates the data-plane fix's live confirmation.

## Prerequisites

- plan-1 + plan-2 merged to `main`; `main` green (CI authoritative).
- Local verify suite green: `go build ./... && go vet ./... && go vet -tags airgap ./... &&
  go test -race ./... && go test -tags airgap ./...`; `cd frontend && npm run build && npx vitest run`.
- `internal/localcompile` golden contract green (mimic TS/Go parity).

## Steps (mirror the beta.13 release runbook)

1. **Pick the version.** Next beta after the current Latest (e.g. `v2.0.0-beta.14`). Confirm the
   current Latest tag first (`gh release view --json tagName`).
2. **CHANGELOG.md** — roll a new `## [2.0.0-beta.14] - <date>` entry covering the full
   `<prev>..HEAD` delta: Fixed = mimic `local=` route-correctness + loopback-egress rejection +
   compile-time guard; Changed = panel theme consistency (node-condition chips, canvas, `--cta`
   Deploy/Compile). Reviewed CHANGELOG PR → merge.
3. **Verify the version stamp** reaches the binary (`-X main.BuildVersion` via the same `release.yml`
   as beta.9–13).
4. **Tag** an annotated `v2.0.0-beta.14` on the green `main` tip; push.
5. **Release + Docker workflows** — confirm `release.yml` (29 assets + sidecars) and `docker.yml`
   both complete success.
6. **Gate on the owner mimic smoke before promoting to Latest.** Because plan-2 is an
   unconfirmable-in-sandbox data-plane change, deliver the runbook (below) and have the owner run it
   on two real hosts. Only after the owner confirms mimic shapes correctly (and nothing regressed for
   a previously-working node) → `gh release edit v2.0.0-beta.14 --latest --prerelease=false`.
   (If the owner prefers, publish as a non-Latest prerelease first, smoke, then promote.)
7. **Verify** `releases/latest` == the new tag; assets present.
8. **Closeout** — STATUS.md, outline plan-status table, memory; archive the subject to `_completed/`
   after the owner's live smoke confirms.

## Owner real-host mimic smoke runbook (deliver in the release notes + `mimic.md`)

Two Linux nodes A,B (Ubuntu 24.04+/Debian 12, kernel ≥6.1, root, public UDP), one `transport:"tcp"`
edge, mimic catalog populated (`MimicVersion`/`MimicReleaseBase`/`MimicDebs` for each
`<codename>-<arch>`).

Pre-flight (each node): `uname -r` (≥6.1); `grep -qw bpf /proc/filesystems`; `command -v jq curl
sha256sum dpkg`.

Deploy: `sudo bash install.sh`. Then assert:
- `systemctl is-active mimic@<egress>` → `active` (`<egress>` = `ip route show default`'s dev).
- `cat /etc/mimic/<egress>.conf` → the new route-correct filter set (per-peer `remote=` or port-keyed
  per the plan-2 design); **never** `local=127.0.0.1:...`.
- `cat /var/lib/yaog-agent/mimic-status.json` → `{"outcome":"active"}`.
- `bpftool prog show` / `tc filter show dev <egress> ingress` → mimic prog/filter attached.
- `wg show` → handshakes non-zero; overlay ping 0% loss.
- **The load-bearing proof:** `sudo tcpdump -ni <egress> port <listenport> -c 20` shows TCP-shaped
  frames WITH mimic; stop the unit → same flow shows UDP.
- **Multi-homing proof (the bug):** on a node with a secondary/floating IP or policy routing, compare
  `ip route get 1.1.1.1` src vs `ip route get <peerIP>` src; confirm the new filter matches the real
  WG source the way the old `local=1.1.1.1-src` did not.
- **Fallback proof:** `mimic_fallback:"udp"` → masking the unit brings WG up as plain UDP +
  `"fell_back_to_udp"`; `"none"` → `exit 1` after `egress_ip_unresolved`/`install_failed`.

## Definition of done

- Beta tagged, `release.yml` + `docker.yml` green, owner smoke passed, promoted to GitHub Latest,
  assets verified.
- Runbook delivered (release notes + `mimic.md`); STATUS + memory updated; subject archived.

## Out of scope

- The mixed-mode release (separate subject + separate beta).

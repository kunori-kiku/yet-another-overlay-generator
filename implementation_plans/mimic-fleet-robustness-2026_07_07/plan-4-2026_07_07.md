# plan-4 — docs

**Goal:** document the rc.4 fixes + the mimic-vs-relay reality.

## Changes
- `docs/spec/artifacts/mimic.md`: the mimic build deps (`bubblewrap` + `dwarves` — the tools
  mimic-dkms's build needs but doesn't declare) added to the install ladder; the unconditional Phase-0
  teardown (tcp→udp de-provisions mimic); the live mimic condition (re-probes the unit each heartbeat);
  a **"mimic needs a direct path"** section — an L7/UDP-accelerator relay terminates+re-originates and
  can't carry the end-to-end fake-TCP (the reverse leg RSTs), so relayed edges use `transport: udp`;
  the `mimic_fallback: udp` unilateral-split caveat + the clean fix (`transport: udp` both ends).
- Bilingual wiki `docs/wiki.md` + `docs/wiki-zh.md`: user-facing — "mimic is for DIRECT links; if a
  link goes through a relay/accelerator, use UDP transport"; the deps are auto-installed now; the panel
  shows live mimic state.

## Verify + branch
Docs-only + a light doc-accuracy agent vs the shipped code. Branch `docs/mimic-fleet-robustness`.

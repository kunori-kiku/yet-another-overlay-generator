# REVIEW-CORRECTIONS — pre-execution subject review (workflow wld2zgik5)

<!-- 18-agent independent review of the drafted subject BEFORE execution: 14 per-plan validators +
     3 cross-cutting lenses + synthesis. Verdict below. These corrections are AUTHORITATIVE and
     supersede the drafted plan specifics where they conflict. Applied during execution. -->

## Verdict: **GO-WITH-FIXES**

> Every one of the 14 plans targets a REAL, HEAD-confirmed defect — no invented work, no wrong-premise plan, nothing rated "flawed"/rethink. Theme coverage is complete (all 14 sweep themes + 6 security findings map to a plan) and the macro tier-sequencing (fixes 1-6 → structural 7-9 → gates/FE 10-11 → doc hygiene 12-14) is coherent with the two genuine hard prerequisites correctly stated (plan-7 after plan-4; plan-14 after plan-2). plan-1 and plan-2 are SOUND as drafted (only anchor-refresh/recipe-pointer tweaks). The other 12 are NEEDS-FIX: each closes a real defect but the plan FILE must be corrected first — chiefly three classes of error: (a) HIGH structural fixes drafted to cover only part of their defect surface (plan-3 patches 1 of 4 SNAT sites and one of two shells; plan-8 mis-identifies dead code; plan-9/plan-10 anchor on the wrong artifact), (b) two plans implement code the skeptic explicitly dispositioned as do-not / doc-note (plan-5 promote-atomicity, plan-10 bidirectional i18n), and (c) custody/safety fixes whose stop-loss does not actually enforce the invariant (plan-3 has no byte-baseline golden; plan-5's lock as worded neither spans the race window nor is reachable cross-package). Four cross-cutting blockers must be reconciled before plan-3/5/9 run, but none invalidates the subject. GO once the plan files are corrected.

## Per-plan corrections (apply before/while executing each plan)


### plan-1 — install.sh coverage guard for artifacts.json — **SOUND**
1. Fix the false 'already integrity-verified above' comment in BOTH install.sh.tmpl:368 AND client-install.sh.tmpl:272 (the mirror carries the same false claim).
2. Nest the new guard inside the existing `if [ -f checksums.sha256 ]` block (install.sh.tmpl:273-282), after `sha256sum -c` succeeds and before any artifacts.json read; mirror placement in the client template. Guard only confirms coverage (grep the manifest) — no redundant hash re-check, since a listed+present artifacts.json is already content-verified above.
3. Specify a shell-exec refusal test (render install.sh, build a bundle with a listed set that passes sha256sum -c PLUS an unlisted artifacts.json, run unprivileged, assert non-zero exit before the id -u root check) — a renderer string-contains check does not prove refusal.
4. Decide deliberately: gate the guard on HasMimic (minimizes golden churn) vs unconditional (defense-in-depth, matches verify.go). Enumerate the golden fixtures needing a reviewed byte-diff regen (localcompile manifest_golden, renderer script/signature goldens, export bundle fixtures) rather than the generic 'if a conformance golden changed'.

### plan-2 — wire Go/WASM build into ship pipelines (release.yml + Dockerfile) — **SOUND**
1. Point the recipe at ci.yml:161-170 (frontend-e2e 'Build WASM' → 'Build panel', the CI-green build:wasm→build ordering), NOT ci.yml:74-96 (which builds wasm only for the vitest roundtrip, never populating dist).
2. For Docker, use the final-stage `COPY --from=backend /src/web/yaog.wasm` + `wasm_exec.js` AFTER the dist COPY (:41); build web/yaog.wasm in the backend stage. Do NOT set GOTOOLCHAIN=local (would ship a 1.25-built wasm against the committed 1.26.5 wasm_exec.js). Note wasm_exec.js provenance/toolchain-resolution in the backend stage so plan-14's base-image bump stays consistent.
3. Make the per-PR ci.yml ship-assertion (`test -f frontend/dist/yaog.wasm && test -f frontend/dist/wasm_exec.js`) NON-optional — the red-build gate in release.yml alone only fires at tag time, so a build-frontend/Dockerfile regression would merge green.
4. Target lib/localEngine.ts (mock the dynamic import to reject-once-then-resolve) for the load-promise reject-recovery test, not wasmEngine (whose harness sets globalThis.yaog so loadWasm never throws). Refresh anchors: ensureWasm :58-65, loadPromise :61-62, stale localEngine import on line 5, .gitignore covers 3 lines incl. wasm_exec.js.

### plan-3 — deploy.go mimic teardown + SNAT correctness + templating — **NEEDS-FIX**
1. BLOCKER: Part A must fix ALL FOUR hard-coded `-s 10.10.0.0/24` exact-match SNAT delete sites (deploy.go:310 bash uninstall, :370 bash CLEAN, :482 PS1 CleanScript, :568 PS1 uninstall) and add mimic teardown to BOTH renderBashDeploy AND renderPS1Deploy — as drafted the HIGH defect ships half-fixed on the entire Windows/clean surface. RenderDeployScripts must derive a HasMimic-class flag from peerMap.
2. Rewrite the mimic teardown to MIRROR install.sh.tmpl:35-42 VERBATIM (HasMimic-gated `systemctl disable --now mimic@<egress>` + `rm -f /etc/mimic/<egress>.conf`, same MimicEgressOverride/MimicEgressInterface egress resolution). Drop the invented bpftool/XDP-detach and /run/mimic removal (that would be a THIRD divergent teardown — the exact drift this plan exists to kill).
3. BLOCKER: Add a characterization golden of RenderDeployScripts (bash + PS1) BEFORE the Part-B templating and gate byte-identity as a RED TEST — no deploy_test.go or deploy golden exists today, so 'byte-identical (reviewed diff)' is currently unverifiable. Correct the Read-first (it cites a non-existent deploy_test.go).
4. Place mimic teardown OUTSIDE the `if !node.IsClient` block, gated only on HasMimic (a client+tcp wg0 has mimic; script.go:433). Route the new MimicEgressInterface field through bashSingleQuote / powerShellArgQuote (operator-controlled free text → remote root shell).
5. Restore the reflect.Map/reflect.Interface recursion hardening to the field_safety_test step (field_safety_test.go:89 `default: return nil` silently skips them) — the outline/T4 assigned it here but the plan dropped it. State up front the PS1 templating half is expected to defer to plan-3.5 (ShellToken == bashSingleQuote only; no PowerShell token constructor).

### plan-4 — agent self-update version-comparator + T5 durability — **NEEDS-FIX**
1. Resolve the step-3 scope contradiction: reword the Read-first note and step 3 to explicitly ENUMERATE the two confirmed T5 gaps to fix — (a) add an io.LimitReader size cap to the download at selfupdate.go:501 matching the package's own 32MB convention (controller_client.go:307), and (b) persist the Attempts bump / surface the SaveState error at :314 so the 3-attempt brick-bound survives a writable-then-RO state dir. Clarify 'only the comparator is broken' means the DESIGN is sound, NOT that these robustness fixes are out of scope.
2. Fix anchors: the comparator to route through is version.go:27 (Compare); :59 is splitVersion (the v-strip); use :415 consistently for the rollback compare.
3. Add regression cases beyond promote+floor-advance: a v-less target that FAILS the health gate still rolls back (sameVersion(build,pu.To) true → .bak restored), and a healthy v-less update that does NOT falsely write abandoned-target/floor bookkeeping. Existing tests all use v-matching versions, so these are non-vacuous. Note the fix RESTORES the AgentVersionFloor anti-downgrade custody (security-relevant, not cosmetic).

### plan-5 — controller store: sign-trustlist serialization + overlay + writeJSONL — **NEEDS-FIX**
1. BLOCKER: Restate step 1 as a NEW internal/controller function holding ONE `defer lockTenantOps(t)()` that spans read(GetCurrentSignedTrustList)+substitution-guard+Verify+PutSignedTrustList, called by the api handler — lockTenantOps is unexported to internal/api (an in-api wrap is impossible) AND serializing only the mutation does NOT close the read-vs-restage (M_old,B_new) window. Cite that storecore methods never re-enter lockTenantOps (bounded deadlock audit).
2. Honor the skeptic disposition on step 2: DOWNGRADE promote crash-atomicity to a doc-note (document the generation-committed-last invariant, storecore.go:470-471) OR, if kept, spell out a concrete promote-in-progress-marker + startup-reconcile design and hard-gate it — do not hand the executor a one-line 'add the crash-atomicity fix' against the delicate promote hot path (raises real risk above the stated 'med').
3. Fix or honestly descope member-2: implement a durable-only read (GetNodeRecord skipping applyTelemetryOverlay) for the RMW writeback callers so GetNode→UpsertNode stops baking a dead node's live 'healthy' conditions to disk (storecore.go:98/114, handler_rekey.go:88-98) — do NOT relabel the real durable leak as 'keep it volatile'.
4. Broaden or explicitly descope member-1's other two handlers (rekey-all/clear-rekey lost-update at handler_rekey.go:87-99) — the Goal overstates 'closes the T6 cluster' while fixing 1 of 3. writeJSONL fix must LOG-and-continue on a post-Write Close() error (preserve the fileLines bookkeeping at :303-320), not early-return.

### plan-6 — WebAuthn User-Verified (0x04) enforcement — **NEEDS-FIX**
1. Rewrite the owner decision-gate to enumerate BOTH (a) operator login/2FA authenticators (recoverable via password first-factor, handler_login.go:109) AND (b) the keystone off-host SIGNING anchor (the fleet-bricking, no-fallback case) SEPARATELY; require a 'no' for ALL before hard-enforce and flag (b) as decisive. The plan's symmetric 'fixes all three' framing inverts the true severity.
2. Document the node-side blast radius: verifyAssertion re-runs on every node via internal/agent/verify.go:354 (trustlist.Verify) on each config fetch — a keystone UV lockout is a FLEET-WIDE config-fetch brick, not just a login lockout. Add the bounding reassurance: a UV-capable authenticator already sets 0x04 under userVerification:'required', so existing manifests keep verifying and hard-enforce is safe exactly when the gate answer is 'no'.
3. Reuse the existing `flagsUPUV = 0x05` (webauthn_test.go:76), add `flagUserVerified = 0x04` next to flagUserPresent, and add a sentinel ErrUserVerification so the negative test (UP-set/UV-cleared, 0x01) pins the rejection REASON as UV. Cite webauthn.ts:390 (shared runAssertion backing keystone-sign + login). Note plan-6.5 (if opened) must be PER-CREDENTIAL, treating keystone re-enroll as a rotation.

### plan-7 — agent daemon decomposition (runControllerMode) + loop tests — **NEEDS-FIX**
1. Replace 'heartbeat coalescing' (ALREADY tested — heartbeat_test.go TestRunHeartbeat_Kick/TestTryKick_NonBlocking) with the actual missing WRAPPER sequencing invariants: single-shot = exactly one cycle + one FinalizeSelfUpdate; daemon apply kicks the heartbeat exactly once (gated on `applied`); an idle/rekey wake does NOT kick; deferred-retry fires only past the interval and never mid-apply. State heartbeat/tryKick tests MOVE with the code, not newly created.
2. Add: collapse cmd/agent's printApplied (main.go:968) into agent.printAppliedTo (cycle.go:225) during extraction (T11 explicitly calls for it). Keep the main.BuildVersion ldflags injection point in cmd/agent — the extracted Daemon/ControllerLoop RECEIVES the version as a constructor field (referenced 6x inside runControllerMode).
3. Add the invariant that the production heartbeat goroutine stays exec-terminated (nil done / no graceful cancel, matching main.go:784 — process image destroyed by syscall.Exec); ctx/done is test-only. Acknowledge e2eagent does NOT drive runControllerMode, so the new sequencing table-tests are the SOLE behavior-preservation gate. Fix anchor ranges (runControllerMode 626-844; Telemetry/Sampler in telemetry.go).

### plan-8 — split derivePeersWithDomains + link-grouping iterator + dead code — **NEEDS-FIX**
1. Rewrite step 3: oriented() is NOT dead (compiler.go:225 write-back calls it with a genuinely-variable isForward). The real dead code is the isForward ELSE-branches — ASSERT isForward==true once in Pass 2, then delete the unreachable branch at peers_build.go:468-472 and the dead body at :563-565 (and de-mislead the :567-594 comments). The grep-zero-callers discipline applies to the separate dead test-only wgInterfaceName wrapper, NOT to oriented().
2. Scope step 2 explicitly: either (a) a shared linkid.ForEachLink iterator consumed by peers Pass1+Pass2 AND the CROSS-PACKAGE validator sites (semantic_ports.go:73, semantic_edges.go:336 — the real DRY value), adding those files to Read-first; or (b) honestly descope the validators, stating the plan single-sources only the 2 in-file loops. Correct the count (~4 clean sites; compiler.go:206-242 write-back iterates un-deduped and CANNOT consume the iterator).
3. Fix the line range (function ends at 628, not 640; helpers proper are 641-826). Land step-2 (dedup) as a SEPARATE commit from step-1 (pure motion) so any golden byte-diff localizes to the dedup. Either fold in or explicitly DEFER the three near-zero-risk ride-alongs (drop redundant enabledEdgeDirections scan; delete dead wgInterfaceName wrapper; formatEndpoint→net.JoinHostPort — flagging the last as byte-touching).

### plan-9 — split handler_bootstrap + shQuote seam + agent-mux auth-adapter — **NEEDS-FIX**
1. BLOCKER: Split step 2. Primitive-share via renderer.ShellQuoted() with a targeted unit test (byte-identical to shQuote). But 'extend field_safety_test to cover bootstrap fields' is INFEASIBLE by that swap — field_safety_test is a reflection walk over CONFIG STRUCTS and renderBootstrapScript imperatively fmt.Fprintf's plain strings (no struct/template), and internal/api does not import internal/renderer. Either DROP the field_safety-coverage claim, or scope a struct+template go:embed refactor (re-rate med, sequence AFTER plan-3 since it edits the SAME shelltoken.go/field_safety_test.go, cross-reference plan-3, add a plan-9.5 marker). Note the OP_FLAGS RPID/ORIGIN are spliced UNQUOTED by design — a third context ShellToken cannot model.
2. Disposition each agent handler explicitly: HandleConfig/HandleReport/HandleTelemetry/HandleRekey → op(); HandlePoll → opRaw (MUST retain the 204 long-poll-deadline contract — op() would emit 200-null and break agent re-poll); HandleEnroll stays hand-rolled (pre-auth, reserves a rate-limit slot before decoding, no identity()). Require a byte-identical response assertion per converted route.
3. Keep the agent-mux change framed as structural-guarantee/hygiene (the in-handler identity() checks are already-dead defense-in-depth behind requireNode), NOT a security fix. Name the concrete preambles (method guard, identity() check, mapControllerErr) instead of the vague 'relay'. Retarget/remove the existing shQuote unit test (handler_bootstrap_test.go:30) so it doesn't fail to compile when shQuote is inlined.

### plan-10 — controller wire-DTO drift gate + bidirectional gates — **NEEDS-FIX**
1. Retarget the gate to the snake_case *JSON/*Wire interfaces in frontend/src/api/controller/*.ts (NodeJSON, SettingsJSON, StageResponseJSON, *Wire) — NOT the camelCase types/controller.ts (nodeId/rpId/publicKeyPEM), whose comparison needs a lossy non-mechanical snake↔camel transform the assessment warned against. The wire is snake_case; compare snake-to-snake for a false-positive-free name-set diff. Mention the cheaper Go-emitted-golden cross-oracle mapper alternative.
2. Enumerate ALL Go controller-DTO sources, not just wire_controller.go: settingsJSON (handler_bootstrap.go:36 — the biggest omission), release_pins.go, telemetry_history.go, cookie_session.go, handler_{totp,login,passkey}.go. State the plan-9 ordering coupling (plan-9 relocates settingsJSON to handler_settings.go), so no hardcoded source path breaks.
3. Scope step 2 to a SCOPED i18n ORPHAN SWEEP ONLY — delete the confirmed-dead error.ts_topology_validation_failed (en.ts:751/zh.ts:731, zero emitters). Do NOT build a naive bidirectional i18n assertion (the ⊆ direction is DELIBERATE, i18n_catalog_sync_test.go:13-16, for error.generic + forward-declared codes — a reverse assertion reds legitimate keys). Drop 'arch allow-list' (layers_test.go:50 is a correct shrink-only ratchet, not a defect) and leave the eslint ratchet.
4. Rename the 10 non-oracle *.conformance.test.ts → plain *.test.ts (safe under vitest's recursive glob; MEMORY note is stale per vitest.config.ts:9-14) and repoint the drift_test.go:42-44 comment to the new gate. Own the TestControllerHTTP_AirGapOpen rename OR (recommended) explicitly hand it to plan-13 — resolve the circular deferral.

### plan-11 — Field.tsx adoption + single-source pin-collision clear payload — **NEEDS-FIX**
1. Rewrite the Read-first: the duplicated pin-clear payload is EdgeEditor.tsx:455-464 (role-change) and :632-640 (unpin), NOT lib/normalizeEdges.ts (whose healCollidingPins is already single-sourced). Cite the EdgeEditor.tsx:441-448 safety comment. EdgeEditor is under components/design/aside/.
2. Single-source the 7 allocation-pin fields against the EXISTING normalizeEdges.PIN_FIELDS (:31-39) and route ALL THREE clear sites through it — the two EdgeEditor sites AND topologyStore.ts:665-671 (purgeModeBoundaryState, a third identical enumeration with its own 'scrub-list-completeness risk' comment). purgeModeBoundaryState is custody-sensitive: only the 7 edge-pin fields unify; the node-secret fields (wireguard_private_key etc.) MUST stay separately enumerated. Note lib/edgeDirection.ts:28-34 also enumerates the set (as a swap).
3. Make the Field DoD NON-absolute: migrate only sites whose label+wrapper+control markup already matches Field's contract; document/leave genuinely-divergent sites (NodeForm's label-less inputs + --content label + text-xs hint; EdgeEditor mono text-xs inputs; DomainForm). Forcing them either changes rendered bytes (violates the behavior-preserving invariant) or needs a reviewed Field extension whose defaults don't alter the 30 migrated sites. Grep each candidate's exact class/label/wrapper strings against FIELD_INPUT_CLASS/LABEL_CLASS/HINT_CLASS before migrating; rate risk LOW-MED. Assign the skeptic-KEPT localEngine vestige delete (LocalEngine type + localEngineEnabled(), zero prod callers) to this plan or plan-14.

### plan-12 — archive 6 delivered plan folders + reconcile STATUS/MEMORY/CHANGELOG — **NEEDS-FIX**
1. Correct the mixed-mode enumeration (the plan's own deliverable is accuracy): merged set is plans 1/2/3/4/6/8 (plan-8 merged as #201, beta.15 — the plan omits it); pending = plan-5 + plan-7 only. The mixed-mode table currently mis-marks plans 1,2,3,4,5,7,8 as 'drafted' and plan-6 as 'remainder pending' though #202 landed it.
2. Add 'Recently closed subjects' entries for ALL FOUR newly-archived-and-unlisted subjects — framework-refactor (MOVE it out of 'Active work'), agent-feedback-and-version-aware-rollout, theme-and-mimic-fixes, and beta16 — not just beta16.
3. In the ~130-line time-capsule prune, PRESERVE the still-live owner-owed items intermixed with the stale stanzas: rc.5 soak (STATUS:426-431) and rc.4-owed controller-update + fleet redeploy + L7-relay udp (STATUS:432-434). Update the STATUS header (line 3 'framework-refactor COMPLETE') and rewrite the 'Two independent tracks' framing (line 413) when the drafted Track-1 stanza is removed. Optionally drop the now-stale 'today at implementation_plans/framework-refactor...' forward-reference in this subject's own outline:114-116.

### plan-13 — purge retired-airgap / deleted-TS-compiler prose + rotted citations — **NEEDS-FIX**
1. Add the under-enumerated drifted docs to the target list: docs/spec/api/http-api.md, docs/spec/api/wire-contract.md, docs/spec/frontend/architecture.md:34-36, docs/spec/compiler/conformance-manifest-schema.md, docs/spec/controller/controller-api.md:8,39-48. Several present the DELETED anonymous routes as the live default surface but contain ZERO 'airgap' literals.
2. Broaden the verification grep to also match the deleted route strings (/api/validate | /api/compile | /api/export | /api/deploy-script), not just 'airgap|air-gap' — otherwise route-citation drift in docs without an air-gap literal survives the purge.
3. Add an explicit CARVE-OUT: PRESERVE the still-live render.AirGap key-custody mode/enum (render.go:43) and its io-contract.md:48 custody-table row; purge only the dead anonymous-HTTP-route framing around it (drop 'HTTP API/API' from render.go:34-35,122 while keeping 'compiler CLI' + the AirGap mode). NEVER rename the Go identifier (invariant 4 = doc/state edits only).
4. Demote refresh-specs from 'preferred' to optional-flat-cache-only — it regenerates only specs/ root and CANNOT fix the nested docs/spec/ drift (which needs hand edits). Split/flag step 6 (add self-update/keystone/telemetry sections to docs/spec/controller/agent.md) as NET-NEW code-cited authoring, not a citation fix. Own the TestControllerHTTP_AirGapOpen rename and the release.yml:18 toolchain comment (1.26.4→1.26.5).

### plan-14 — Release/Docker pipeline hygiene + dead-code sweep — **NEEDS-FIX**
1. Reframe the DoD/invariant: pipeline/CI/build/TAGGING behavior changes ARE in scope (the docker.yml latest-tag predicate alignment and the Dockerfile golang:1.25→1.26-alpine bump both deliberately change behavior) — only PRODUCT/runtime/generated-config behavior is out. Distinguish the two so a reviewer does not reject the docker-latest/base-image fix as 'behavioral, belongs elsewhere'.
2. Carry the T14 skeptic dispositions: member 3 (delete unsigned RenderInstallScript/RenderClientInstallScript) is churn-not-debt → KEEP (~9 test callers at HEAD); member 4 (GetCurrentSignedTrustList rename) is OPTIONAL touching ~16 sites (29 refs), not '~7' — mark deferred so the executor doesn't do the rejected churn.
3. Explicitly DEFER release.yml:18 to plan-13 (double-claimed); land this plan AFTER plan-2 (shared release.yml/Dockerfile). Add a step: grep frontend/e2e + unit tests for bg-green-400/yellow-400/red-400 status-dot locators BEFORE the WireGuardPeersPanel token swap (MEMORY theme-refactor lesson). Clarify the conformance→test npm rename touches ci.yml:101 run command only, NOT the required 'WASM conformance gate' job display name (no branch-protection change). Mirror the latest-tag predicate INLINE into docker.yml:57 (no cross-workflow shared expression exists).

## Cross-cutting blockers (reconcile before plan-3 / plan-5 / plan-9)


- **[BLOCKER]** plan-3's HIGH teardown-correctness fix ships half-fixed AND rests on an unaudited reference. As drafted it patches only 1 of 4 hard-coded 10.10.0.0/24 SNAT delete sites (deploy.go:310, missing :370/:482/:568) and only the bash uninstall builder — the PowerShell deploy path and clean scripts still orphan mimic + leave stale POSTROUTING rules on the entire Windows/clean surface. It also mirrors install.sh.tmpl's mimic teardown as ground truth, but mimic runtime/DKMS lifecycle was NEVER actually audited (Part B covered 6 areas; mimic is not among them, yet the coverage ledger marks it 'closed by Part B'). And the byte-identity gate for the templating half has NO baseline — deploy_test.go and any deploy golden do not exist.
  - **Resolution:** Before plan-3 runs: (1) add an independent read of install.sh.tmpl:35-42 mimic teardown as a Read-first step (do not assume correct); (2) fix all 4 SNAT sites + add mimic teardown to BOTH renderBashDeploy and renderPS1Deploy, gated on a HasMimic-class flag derived from peerMap, placed outside the !IsClient block; (3) capture a RenderDeployScripts characterization golden and gate byte-identity as a RED test before touching the builders; (4) correct the ASSESSMENT ledger to mark the mimic-lifecycle bullet NOT-closed.

- **[BLOCKER]** plan-5's custody sign-trustlist fix, as literally worded ('wrap the manifest-mutation under lockTenantOps'), does NOT close the (M_old,B_new) fail-closed fleet-strand race and is not even reachable: lockTenantOps is unexported to internal/controller while HandleTrustListSignature lives in internal/api, and the race is between the READ (stagedManifest) and the WRITE (PutSignedTrustList) — serializing only the write leaves the window open.
  - **Resolution:** Reframe as a new internal/controller sign method holding ONE lockTenantOps acquisition spanning read+substitution-guard+Verify+PutSignedTrustList (mirroring compile_stage.go / compile_promote.go), called by the api handler. Cite that storecore methods never re-enter lockTenantOps so the audit is bounded (no reentrancy deadlock).

- **[BLOCKER]** plan-9's 'route shQuote through the ShellToken seam + extend field_safety_test to cover bootstrap fields' is architecturally infeasible as the LOW-risk mechanical change it is rated, and it silently collides with plan-3 on the same renderer seam (shelltoken.go / field_safety_test.go) with zero coordination. renderBootstrapScript is an imperative fmt.Fprintf in internal/api (no struct, no template), internal/api does not import internal/renderer, and ShellToken confers no structural guarantee without a go:embed template move — the same 'med' class of work plan-3 does.
  - **Resolution:** Either (a) descope plan-9 to an api-local renderer.ShellQuoted() primitive-share with its own unit test and DROP the field_safety-coverage claim; or (b) scope the cross-package go:embed template move, re-rate it med, sequence it explicitly AFTER plan-3, and state it rebases on plan-3's shelltoken.go/field_safety_test.go edits. Add an explicit cross-reference between plan-3 and plan-9 naming the shared seam, plus a plan-9.5 marker for the OP_FLAGS word-split fields that don't fit the quoted-arg seam.

- **[MAJOR]** Two plans implement code the T6/T7 skeptic explicitly dispositioned as do-NOT / doc-note, adding regression risk on delicate paths for low value against their own evidence base: plan-5 step 2 elevates promote crash-atomicity to an atomic-snapshot code change on the custody promote hot path (skeptic: downgrade to a doc-note), and plan-10 step 2 names i18n as a 'make bidirectional' example (skeptic: the ⊆ direction is deliberate; a naive reverse assertion REDS legitimate error.generic + forward-declared keys, breaking the green build).
  - **Resolution:** Honor the dispositions: plan-5 step 2 → doc-note (or a fully-specified promote-in-progress-marker + startup-reconcile design if kept, hard-gated); plan-10 step 2 → a SCOPED i18n orphan sweep only (delete the confirmed-dead error.ts_topology_validation_failed), with a naive bidirectional i18n assertion explicitly excluded and 'arch allow-list' dropped.

- **[MAJOR]** Member-level coverage orphans: skeptic-KEPT items with no assigned home. (1) The ~5-line localEngine vestige delete (LocalEngine type + always-true localEngineEnabled(), zero prod callers) is unassigned. (2) plan-3 dropped the T4 field_safety reflect.Map/reflect.Interface recursion hardening between outline and plan. (3) The orphan i18n key + edgeDirection.ts:8/:40 stale citations (to deleted ../compiler/peers.ts and pre-split peers.go) are unassigned. (4) plan-5 member-2 durable overlay leak is preserved-as-volatile rather than fixed.
  - **Resolution:** Pin each to exactly one plan: localEngine delete → plan-11 or plan-14; field_safety map/interface recursion → back into plan-3 step 4; orphan i18n key → plan-10's scoped sweep; edgeDirection citation repoint → plan-13; and decide plan-5 member-2 (implement GetNodeRecord durable-only read OR explicitly, honestly defer it).

- **[MINOR]** Minor sequencing/ownership coordination gaps that do not block but risk merge conflicts or a re-touched file: (1) circular ownership of the TestControllerHTTP_AirGapOpen rename (plan-10 defers to plan-13 and vice-versa — assigned to nobody); (2) release.yml edited by plans 2/13/14 and Dockerfile by 2/14, with the in-Docker wasm build landing on golang:1.25-alpine (force-pulling go1.26.5) BEFORE plan-14 bumps the base image, and wasm_exec.js provenance left implicit; (3) plan-2's optional ci.yml ship-gate would add an 8th required check without flagging the branch-protection-same-PR rule that plan-10/plan-14 already carry.
  - **Resolution:** Assign the AirGapOpen rename solely to plan-13 (plan-10 becomes a pure pointer). Sequence plan-14 after plan-2, have plan-2 state the backend-stage wasm_exec.js source + go1.26.5 toolchain resolution (and NOT GOTOOLCHAIN=local), keep the plan-13 :18 comment and plan-14 alignment non-overlapping. If plan-2 adds a ci.yml gate, require the same-PR branch-protection PATCH (full list, app_id:15368) note.
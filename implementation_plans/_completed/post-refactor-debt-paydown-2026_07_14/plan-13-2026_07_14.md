# plan-13 — Purge retired-airgap / deleted-TS-compiler prose + rotted citations (LOW)

- **Subject:** post-refactor-debt-paydown-2026_07_14 · **Tier:** 4 (doc hygiene)
- **Status:** pending · **Branch:** `docs/purge-airgap-ts-prose`
- **Reads from specs:** (this plan EDITS `specs/` — read `specs/README.md` first)

## Prerequisites
None. Prose only (invariant 4 — no behavior change).

## Goal
framework-refactor deleted the anonymous air-gap API, the TS compiler, and the conformance harness, but
a large surface of authoritative prose still presents them as live — misleading humans AND automated
executing sessions (the flat `specs/` cache is loaded as ground truth by `execute-implementation-plan`).
Purge it.

## Read first
- `specs/airgap-api.md` — documents the DELETED anonymous `/api/{validate,compile,export,deploy-script}`
  routes/handlers as the live default surface, with broken line citations; `specs/README.md` — the
  reading guide + the AIRGAP node/cross-refs to strip.
- `README.md:8,34` — the headline feature bullet still sells the deleted TypeScript compiler +
  conformance gate + build-tag airgap, two lines above text saying they were removed.
- `CLAUDE.md` (repo) — the API-endpoints section still documents the retired `//go:build airgap`
  boundary + the four anonymous routes as live.
- `RELEASING.md` — build-version injection in future tense (live since beta.1).
- `docs/spec/compiler/io-contract.md` + `internal/localcompile/contract.go` + `internal/edgecase/*` +
  the keygen doc-comments — framed around the gone TS twin / conformance harness / air-gap handlers,
  incl. a factually-wrong live-caller list.
- `internal/api/*` — stale air-gap comments naming deleted routes/handlers as live; the lying
  `TestControllerHTTP_AirGapOpen` test name (coordinate the rename with plan-10 so it is done once).
- `docs/spec/controller/agent.md` — the canonical, code-cited agent spec OMITS the self-update /
  keystone / telemetry lifecycle (add it).
- `.github/workflows/release.yml:18` — the stale `go1.26.4` toolchain comment (go.mod pins 1.26.5).
- ASSESSMENT.md **T2** (the full drifted-doc member list, each with the specific stale claim).

## Implementation steps
1. Re-run `refresh-specs` against HEAD (preferred — regenerates the whole flat cache correctly), OR
   delete `specs/airgap-api.md` + strip the AIRGAP nodes/cross-refs from `specs/README.md` and adjacent
   component files.
2. Rewrite README:8/34, CLAUDE.md's API-endpoints section, RELEASING's build-version section, and the
   `localcompile`/`edgecase`/`keygen`/`io-contract` doc-comments to describe the WASM-in-browser +
   single-controller-build reality (no TS twin, no conformance harness, no anonymous airgap routes). Fix
   the factually-wrong live-caller claim.
3. Rename `TestControllerHTTP_AirGapOpen` to what it actually asserts (coordinate with plan-10); fix the
   stale `internal/api` air-gap comments.
4. Replace frozen absolute line-number citations (across docs + the FE stores slice comments) with stable
   symbol references, so a future split does not rot them again.
5. Fix the `release.yml:18` toolchain comment (1.26.4 → 1.26.5).
6. Add the missing agent self-update/keystone/telemetry lifecycle sections to `docs/spec/controller/
   agent.md`.

## Tests produced
None (prose). If `refresh-specs` is run, the specs cache regenerates — review the diff, do not blind-accept.

## Verification
`grep -rin 'airgap\|air-gap\|//go:build airgap\|TypeScript compiler\|conformance harness'` across docs +
comments returns only accurate/historical references (no "live default surface" framing); no doc cites a
deleted route/handler/file as current; spot-check that replaced citations resolve. No behavior change:
`go build ./... && go test ./...` green (comment/name edits only).

## Definition of done
- No authoritative prose presents the deleted airgap API / TS compiler / conformance harness as live;
  the lying test name renamed; frozen citations → symbol refs; the release.yml toolchain comment + the
  agent spec lifecycle fixed. Review → fix → re-review clean → CI green.

## Out of scope
Any behavior/logic change. The bilingual wiki full rewrite (a larger doc subject if desired). Deleting
`internal/edgecase` code (it has test callers — comments only).

## Commit template
```
docs: purge retired-airgap + deleted-TS-compiler prose and rotted citations (#PR)

The framework-refactor deleted the anonymous airgap API, the TS compiler, and the
conformance harness, but specs/airgap-api.md, README, CLAUDE.md, RELEASING, the
localcompile/edgecase/io-contract doc-comments, and stale api comments still present
them as live (and execute-implementation-plan loads specs/ as ground truth). Regenerate
specs/, rewrite the drifted prose to the WASM/single-build reality, rename the lying
TestControllerHTTP_AirGapOpen, replace frozen line-refs with symbol refs, and fix the
release.yml toolchain comment. Prose only.
```

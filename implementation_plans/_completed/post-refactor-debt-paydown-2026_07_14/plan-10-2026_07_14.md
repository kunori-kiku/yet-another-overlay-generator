# plan-10 — Wire-DTO camelCase drift gate + conformance-name honesty + bidirectional gates (LOW–MED)

- **Subject:** post-refactor-debt-paydown-2026_07_14 · **Tier:** 3 (machine-gate completion)
- **Status:** pending · **Branch:** `feat/camelcase-dto-drift-gate`
- **Reads from specs:** `api-wire-contract` (`docs/spec/api/wire-contract.md`), `model-validation`

## Prerequisites
None. (Extends framework-refactor plan-10's `internal/wiredrift` on `main`.)

## Goal
The framework-refactor drift gate (`internal/wiredrift`) covers the `omitempty` / `*_OMITEMPTY` /
`PIN_FIELDS` lists and the snake_case wire DTOs — but NOT the camelCase controller wire DTOs
(operator/agent API), which are still hand-mirrored TS↔Go with no red-build gate. Close that last
convention-held contract, and fix the one-directional gates + dishonest names the sweep found.

## Read first
- `internal/wiredrift/drift_test.go` (366) + `doc.go` — the existing AST-based gate (how it walks Go
  json tags + the FE mirror lists; the non-vacuous self-test).
- `internal/api/wire_controller.go` + `frontend/src/types/controller.ts` — the camelCase controller DTOs
  hand-mirrored across the boundary (the ungated pair).
- `internal/i18n_catalog_sync_test.go` + `internal/arch/layers_test.go` — the gates the sweep flagged as
  one-directional (do they catch BOTH add + remove?).
- ASSESSMENT.md **T7** (the camelCase DTO gap, conformance-name honesty, and the one-directional gates).

## Implementation steps
1. Extend `wiredrift` (or add a sibling AST gate) to cross-check the camelCase controller wire DTOs (Go
   struct json tags ↔ the TS `controller.ts` interface fields) so a rename / added / dropped field on
   either side REDS the build. Reuse the go/ast approach; prove it non-vacuous (mutate → red).
2. Make any one-directional gate bidirectional where it should be (e.g. an i18n or arch allow-list that
   only catches growth but not a silent removal).
3. Correct dishonest conformance/test names the sweep flagged (rename to reflect what they actually
   assert). (The `TestControllerHTTP_AirGapOpen` rename is in plan-13's doc-honesty scope — coordinate
   so it is not done twice.)

## Tests produced
- **CI gate (perpetual):** the camelCase DTO drift check + its non-vacuous self-test.

## Verification
`go test ./internal/wiredrift/... ./internal/...`; mutate a controller DTO field name → confirm the gate
REDS; `go vet ./...`; `gofmt -l`. If a new CI job/name is added, update branch protection in the SAME
PR.

## Definition of done
- The camelCase controller DTOs are drift-gated (proven non-vacuous); one-directional gates fixed;
  dishonest names corrected. Review → fix → re-review clean → CI green.

## Out of scope
The FE regex error framework (deliberately distinct from Go apierr). A codegen rewrite of the DTOs (a
drift gate is the accepted mechanism, not codegen).

## Commit template
```
feat(wiredrift): gate the camelCase controller wire DTOs against drift (#PR)

The drift gate covered the snake_case wire DTOs + omitempty lists but not the
hand-mirrored camelCase controller DTOs (operator/agent API), the last convention-held
contract. Extend the go/ast gate to cross-check controller.ts against the Go json tags
(non-vacuous), make the one-directional gates bidirectional, and correct dishonest
test names.
```

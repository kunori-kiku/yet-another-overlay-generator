# plan-5 — release v2.0.0-rc.4

**Goal:** cut `v2.0.0-rc.4` (mimic fleet-robustness: build deps + teardown + live condition + relay
warning + docs).

## Steps
- CHANGELOG roll (`v2.0.0-rc.3..HEAD`): Fixed — mimic build deps (bubblewrap+dwarves; the DKMS build
  now succeeds on current Debian kernels), unconditional Phase-0 teardown (tcp→udp de-provisions
  mimic), live mimic condition (panel reflects the real unit state, not the deploy-time breadcrumb);
  Added — `relay-path` mimic warning (mimic needs a direct path). Add the `rc.3...rc.4` compare link.
- Full integrated verify (both Go profiles + `-race`, FE build + vitest + lint + gofmt).
- Reviewed CHANGELOG PR → merge → green tip → annotated tag `v2.0.0-rc.4` (rc self-promotes to Latest).
  Verify: Release run green, ~29 assets, sidecar hash + published `version` = `v2.0.0-rc.4`, Latest.
- STATUS + memory closeout; `git mv` the subject to `_completed/`.
- Owner apply: `apt-get install -y bubblewrap dwarves` is now automatic on redeploy; confirm a
  current-kernel Debian node builds mimic on a clean deploy; confirm tcp→udp stops mimic; confirm the
  panel shows a stopped mimic live; confirm the relay-path warning fires on a relayed tcp edge.

## Gate
A defect during the rc.4 soak → rc.5 under the same gate rules.

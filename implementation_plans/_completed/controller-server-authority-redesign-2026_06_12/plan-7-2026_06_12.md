# plan-7 — Docs, migration notes, closure smoke
<!-- drafted: 2026-06-12 -->
Outline: [`outline.md`](outline.md) · Prerequisites: plans 1–6 merged

## Goal

Bring every document in line with the shipped reality, publish the breaking-change migration
note, run the full owed manual smoke on a two-node fleet, and close the subject via
`/close-phase`.

Reads from specs: controller-store, controller-operator-api, controller-agent-api, panel-auth
(touch-ups to these four files are part of this plan's deliverable)

## Read first

1. `README.md` (controller section + configuration reference — written 2026-06-12, now stale
   in: env table row for the old prefix var, "Open the panel" walkthrough pre-login-gate,
   plain-HTTP note).
2. `docs/spec/controller/persistence.md` (the public-keys-only claim — now TRUE; rewrite the
   §Stored shapes guarantee wording + add history layout), `operator-auth.md` (login gate),
   `controller-api.md` (§The two ports + the two prefixes), `docker.md` (env table),
   `key-custody.md` (storage-boundary enforcement note), `deploy.md` (audit entries now real).
3. `specs/controller-store.md` (Invariants: doc-drift bullet flips to enforced),
   `specs/controller-operator-api.md`, `specs/controller-agent-api.md` (prefix split),
   `specs/panel-auth.md` (LoginPage), `specs/README.md` glossary (secret path prefix entry).
4. `STATUS.md` owed-smoke checklist (carried since the keystone program).

## Implementation steps

1. **README.md**: config table — replace the prefix row with the two new vars; controller
   walkthrough — login-gate flow ("you land on a login page"), hydration behavior, the stash;
   migration callout box: env rename + first-login overwrite + re-login requirement.
2. **docs/spec/controller/** pass per the read-first list — every claim verified against the
   merged code (the persistence.md guarantee may now cite the 400 path + perpetual test).
3. **specs/ touch-ups** (light edits, not a refresh-specs run): the four files above + README
   glossary; update `<!-- last-verified -->` stamps on touched files only.
4. **Migration note**: `docs/MIGRATION-controller-server-authority.md` — env rename table,
   first-login hydration/stash behavior, enrollment-dedupe 409 semantics, promote-scoping
   change; linked from README.
5. **Closure smoke** (the owed two-node controller deploy, extended): browser + authenticator,
   `http://localhost` WebAuthn caveat; verify all eight Mission success criteria from the
   outline + login-survives-refresh + dark/light + no token in localStorage (carried items);
   record the transcript in the closure notes.
6. **Close**: update the outline status table, then invoke `/close-phase` (archives to
   `_completed/`, regenerates STATUS.md, proposes the memory note + notification).

## Verification

The closure smoke IS the verification. Docs: every code-behavior claim spot-checked against
merged main; `grep -rn YAOG_CONTROLLER_PATH_PREFIX .` hits only the migration note.

## Tests produced by this plan

None (docs + manual smoke). Test-lifecycle bookkeeping: confirm plan-1/2/3/6 subject-scoped
tests are listed for retirement in the close-phase checklist; perpetual tests
(topology_custody, store-compat history, cycle_idle, enrollment_dedupe) stay.

## Definition of done

- [ ] All docs accurate against merged main; migration note published and linked.
- [ ] specs/ touched files re-stamped; STATUS.md regenerated by close-phase.
- [ ] Two-node smoke transcript recorded; all Mission success criteria checked.
- [ ] Subject archived to `_completed/`; live deployment fully migrated and verified.

## Out of scope for this plan

Any code change beyond what the smoke reveals as a blocker (a blocker ⇒ STOP, outline update,
insertion-point plan per convention — not a silent fix inside the docs plan).

# plan-4 — docs + behavioral proof

**Goal:** document the module-build reality + the new failure classification + the egress override +
the capability probe; prove the honor-policy behavior.

## Changes
- `docs/spec/artifacts/mimic.md`: expand the "Install ladder" + "UDP fallback" sections — the real
  success gate is a **built + loadable kernel module** (not just the `mimic` binary); the new
  `module_unavailable` outcome/condition + when it fires (stale kernel: `linux-headers-$(uname -r)`
  pruned from the repo → DKMS stuck at `added`); the reboot-into-current-kernel remediation; the
  orphaned-lock cleanup + `modprobe` on start; the per-node egress-interface override; the pre-deploy
  capability probe. Correct any claim that implies the binary install is sufficient.
- Bilingual wiki `docs/wiki.md` + `docs/wiki-zh.md` §mimic: user-facing — "if a link won't come up on
  a mimic node, its kernel is likely behind the repo; reboot into the current kernel, or set Mimic
  fallback → UDP"; the egress-interface field; the node-editor mimic-capability warning.
- Behavioral proof: the plan-1 renderer/conformance goldens (module gate + honor-policy + lock
  cleanup) are the byte-proof; note that a real DKMS build/module-load can't run in netns CI (the gap
  that let this ship), so the goldens + the agent-probe unit tests are the standing guard.

## Verify + branch
Docs-only; a light doc-accuracy agent vs the shipped code (like the rc.2 docs PR). Branch
`docs/mimic-runtime-reliability`.

# plan-5 — release v2.0.0-rc.3

**Goal:** cut `v2.0.0-rc.3` (fixes the rc.2-soak mimic runtime/module defect + adds the egress
override + capability probe).

## Steps
- CHANGELOG roll (`v2.0.0-rc.2..HEAD`): Fixed — mimic module build/load verification (the exit-22
  root cause) + honor-policy (closes the silent no-degrade on `udp`), orphaned-lock cleanup (exit-17),
  `modprobe` on start; Added — per-node egress-interface override, pre-deploy mimic-capability probe,
  `module_unavailable` condition. Add the `rc.2...rc.3` compare-link footer.
- Full integrated verify (both Go profiles + `-race`, FE build + vitest + lint + gofmt).
- Reviewed CHANGELOG PR → merge → green tip → annotated tag `v2.0.0-rc.3` (rc self-promotes to Latest;
  no `-beta.`/`-preview.` substring). Verify: Release run green, ~29 assets, sidecar hash + published
  `version` = `v2.0.0-rc.3`, `gh release list` shows Latest.
- STATUS + memory closeout; `git mv` the subject to `implementation_plans/_completed/`.
- Hand the owner the re-smoke: on a CURRENT-kernel tcp node confirm the module builds + mimic active;
  on a stale-kernel node confirm `udp` policy → plain UDP (link up) and `none` policy → clean
  fail-closed with the reboot message (no exit-22 loop); confirm the panel's pre-deploy capability
  warning fires on the stale-kernel node; test the egress override on a multi-homed node.

## Gate
A defect found during the rc.3 soak → rc.4 under the same rules.

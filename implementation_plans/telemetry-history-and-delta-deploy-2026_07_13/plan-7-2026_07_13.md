# plan-7 — docs (spec + bilingual wiki)

**Goal:** document both tracks.

## Changes
- **Spec:** a new `docs/spec/operations/telemetry-history.md` (sampler fields incl. `cpu_pct`
  semantics + first-beat gap; the ring/flush/cap design + the heartbeats-never-write invariant; the
  query API + clamping) and delta-deploy sections in the controller/operations spec (skip identity =
  checksums digest; generation semantics; Force; preview; the keystone mixed-generation proof).
- **Wiki (`docs/wiki.md` + `docs/wiki-zh.md`, faithful mirror):** node-detail charts (ranges,
  granularity, the cap setting + default, "history off" state); Deploy dialog changed/unchanged +
  Force (when to use it: on-host drift/rescue — content changes never need it); the telemetry
  condition table row if `resource` gains cpu.
- Update `docs/spec/…/telemetry` / conditions docs where `hostResource` is described.

## Verify + branch
Docs-only + a doc-accuracy agent pass vs the shipped code (the rc.4 pattern).
Branch `docs/telemetry-history-delta-deploy`.

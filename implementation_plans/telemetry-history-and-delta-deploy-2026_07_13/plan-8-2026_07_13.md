# plan-8 — release v2.0.0-rc.5

**Goal:** cut `v2.0.0-rc.5` (telemetry history + charts + delta deploy).

## Steps
- CHANGELOG roll (`v2.0.0-rc.4..HEAD`): Added — resource history (raw log + configurable cap),
  history API, node-detail charts (Recharts, granularity picker), `cpu_pct`, delta deploy
  (skip-unchanged + Force + pre-deploy preview). Compare links (rc.4…rc.5 + Unreleased bump).
- Full integrated verify (Go default+airgap+`-race`; FE build + vitest + lint + e2e; gofmt).
- Reviewed CHANGELOG PR → merge → green tip → annotated tag `v2.0.0-rc.5` (rc self-promotes) →
  verify run green, asset count + sidecars, Latest pointer, published version string.
- STATUS + memory closeout; `git mv` the subject to `_completed/`.
- **Owner smoke:** deploy an unchanged topology → "0 updated, N unchanged", NO node refresh
  (`wg show` handshakes undisturbed); change one node → only it refreshes; Force redeploys an
  unchanged node; charts render at several granularities; cap setting honored.

## Gate
A defect during the rc.5 soak → rc.6 under the same rules.

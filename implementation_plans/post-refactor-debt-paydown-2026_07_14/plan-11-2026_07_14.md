# plan-11 — Finish plan-10 Field adoption + the real pin-collision safety dup (LOW–MED)

- **Subject:** post-refactor-debt-paydown-2026_07_14 · **Tier:** 3 (tight FE)
- **Status:** pending · **Branch:** `refactor/finish-field-adoption`
- **Reads from specs:** `panel-design`

## Prerequisites
None. (Finishes framework-refactor plan-10's `ui/Field.tsx` migration.)

## Goal
framework-refactor plan-10 introduced `ui/Field.tsx` and migrated 30 form sites; the sweep found
stragglers still hand-rolling the same label+input markup, plus a genuine **pin-collision safety logic
duplicated** on the FE. Finish the adoption and dedup the real logic — WITHOUT the full FE
component-tree / `lib/` re-folder (decision 2: churn, out of scope).

## Read first
- `frontend/src/ui/Field.tsx` + `Field.test.tsx` — the primitive + its contract.
- The migrated sites (grep `from '../ui/Field'` / `<Field`) vs the stragglers still hand-rolling
  `<label>…<input className=…>` — the remaining migration surface (EdgeEditor/NodeEditor/NodeForm/
  MimicCatalogSettings/AgentUpdateSettings are the large editors to check).
- `frontend/src/lib/normalizeEdges.ts` (168) + the pin-collision heal logic — where the FE duplicates a
  pin-collision safety check (find the second site).
- ASSESSMENT.md **T10** (the genuine part) + **T13** (the OVERSTATED FE-restructure — NOT this plan).

## Implementation steps
1. Migrate the remaining hand-rolled form-field sites to `<Field>` — pure refactor, rendered output
   byte-identical (the plan-10 discipline: markup must match, or it is a visual regression).
2. Dedup the pin-collision safety logic into one helper and route both sites through it.
3. **OPTIONAL** (only if a clean seam falls out during the above; not a goal): extract a narrow slice of
   `topologyStore.ts` (e.g. the compute/persistence actions) — but do NOT undertake the full slice or
   the `lib/` refolder (T13, out of scope). If no clean seam appears, skip.

## Tests produced
- **Perpetual:** a vitest for the deduped pin-collision helper; extend `Field.test.tsx` if a new prop is
  needed.

## Verification
`cd frontend && npm run build` (`tsc -b`) `&& npx vitest run && npm run lint`;
`npm run test:e2e -- --grep-invert "visual corpus"` (no interaction regression); spot-check 2-3 migrated
sites render identically (compare DOM/classes).

## Definition of done
- No straggler hand-rolls the Field markup; the pin-collision safety logic is single-sourced; rendered
  output unchanged; e2e green. Review → fix → re-review clean → CI green.

## Out of scope
The full `topologyStore` slice, the god-component decomposition, the `lib/` refolder (T13 — churn,
decision 2). Any behavior change to compile/validate/deploy.

## Commit template
```
refactor(fe): finish Field adoption + single-source the pin-collision safety check (#PR)

plan-10 migrated 30 form sites to ui/Field; the sweep found stragglers still hand-rolling
the label+input markup, plus a pin-collision safety check duplicated on the FE. Migrate
the stragglers (rendered output byte-identical) and dedup the safety check into one
helper. No component-tree/lib refolder (out of scope).
```

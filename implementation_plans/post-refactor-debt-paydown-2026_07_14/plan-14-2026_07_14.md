# plan-14 — Release/Docker pipeline hygiene + misc straggler cleanup (LOW)

- **Subject:** post-refactor-debt-paydown-2026_07_14 · **Tier:** 4 (build hygiene + misc)
- **Status:** pending · **Branch:** `chore/pipeline-hygiene-and-misc`
- **Reads from specs:** `docs/spec/operations/ci-cd.md`

## Prerequisites
Land plan-2 first (it owns the wasm-in-pipeline wiring); this cleans up the REMAINING pipeline items so
they do not conflict.

## Goal
The T8 release/Docker pipeline hygiene items NOT folded into plan-2, plus the T14 misc stragglers
(confirmed dead code, remaining lying names, cosmetic leftovers). Opportunistic, low-risk batch.

## Read first
- `.github/workflows/release.yml` + `docker.yml` — the latest-tag / `make_latest` policy (the
  owner-established `-beta.`/`-rc.` self-promotion pattern) + any dead step.
- `Dockerfile` + `go.mod` (`toolchain go1.26.5`) — the base-image ↔ go.mod toolchain alignment note (the
  rc.x backlog item: the Dockerfile Go base vs the go.mod pin).
- `scripts/` — any dead script (e.g. a wasm/soak helper superseded by plan-2, or a script referencing a
  deleted path).
- ASSESSMENT.md **T8** + **T14** — the specific pipeline items + the misc dead-code / lying-name list
  (confirm each is still present + still dead before deleting — the confirm-before-delete discipline).

## Implementation steps
1. Apply the T8 pipeline items not in plan-2: the latest-tag policy tidy, the base-image ↔ go.mod
   toolchain alignment, and removal of any dead script/step (each verified dead first).
2. Apply the T14 misc: delete confirmed-dead code (grep zero callers, prod + tests), fix remaining lying
   names, clear cosmetic stragglers. Batch what has not landed in plans 1-13.
3. Do NOT fold in anything behavioral — if a "misc" item turns out to change behavior, it belongs in its
   own reviewed plan, not here.

## Tests produced
None (hygiene). Keep existing gates green.

## Verification
`go build ./... && go vet ./... && go test ./...`; `cd frontend && npm run build && npm run lint`; a
release-workflow dry-run/read confirms the pipeline still tags + promotes correctly; `gofmt -l`. If any
CI job/name changed, update branch protection in the SAME PR.

## Definition of done
- The remaining pipeline hygiene + misc stragglers are cleared; every gate green; no behavior change.
  Review → fix → re-review clean → CI green. (This is the last plan — hand off to `close-phase` for
  subject closure + archival.)

## Out of scope
Anything behavioral. The FileStore SPOF / Dockerfile-HA (architectural, deferred). The bilingual wiki
rewrite.

## Commit template
```
chore(ci): pipeline hygiene (latest-tag, base-image toolchain) + misc dead-code sweep (#PR)

Clean up the T8 release/Docker items not covered by the wasm-ship fix (latest-tag
policy, Dockerfile base-image vs go.mod toolchain alignment, dead scripts) and the T14
misc stragglers (confirmed dead code, lying names). Verified-dead before deletion; no
behavior change.
```

# plan-12 — implementation_plans/ + STATUS archival reconciliation (LOW, bookkeeping)

- **Subject:** post-refactor-debt-paydown-2026_07_14 · **Tier:** 4 (planning-state hygiene)
- **Status:** pending · **Branch:** `docs/archive-stale-subjects`
- **Reads from specs:** (none — planning-state, not architecture)

## Prerequisites
None. Pure bookkeeping — zero code risk, high friction-relief for every resuming session.

## Goal
The state-tracking spine future sessions resume from is internally contradictory and stale: six
delivered subjects (incl. framework-refactor itself) were never `git mv`'d to `_completed/`; STATUS.md
calls framework-refactor both COMPLETE and DRAFTED and lists ~130 lines of falsified IN-PROGRESS
time-capsules; the ONLY genuinely-active subject (mixed-controller-local-mode) is mis-tracked in three
places; and CHANGELOG is missing seven footer compare-links. Reconcile all of it.

## Read first
- `implementation_plans/` (active area) — the six delivered-but-unarchived folders: `framework-refactor-
  2026_07_13`, `agent-feedback-and-version-aware-rollout-2026_06_22`, `beta9-smoke-hardening-2026_06_23`,
  `pre-rc1-2026_06_18`, `theme-and-mimic-fixes-2026_06_25`, and `beta16-smoke-hardening-2026_06_27`
  (VERIFY beta16's status — STATUS never mentions it; confirm delivered vs abandoned before archiving).
- `implementation_plans/mixed-controller-local-mode-2026_06_25/outline.md` — its plan-status table marks
  plans 2/3/4 "drafted" though git shows them merged/shipped (beta.15); its Read-first anchors point at
  pre-split line numbers (the god-file splits moved them → `compile_subgraph.go`/`compile_manualnode.go`/
  `handler_topology.go`). Only plan-5 (telemetry reporter) + plan-7 (closure) genuinely remain.
- `STATUS.md` — the framework-refactor COMPLETE-vs-DRAFTED contradiction (`Active work` vs `Next actions`
  Track 1); the stale time-capsules; the missing beta16 entry.
- `~/.claude/projects/-home-kunorikiku-source-yet-another-overlay-generator/memory/MEMORY.md` +
  `framework-refactor-shipped.md` — reconcile with the archival.
- `CHANGELOG.md` footer — the missing beta.10–16 compare-link definitions.
- `implementation_plans/README.md` — the closure ritual (status prefix + test migration on `git mv`).

## Implementation steps
1. `git mv` each delivered subject folder into `implementation_plans/_completed/` (fixing
   theme-and-mimic-fixes' own outline status table first if it is stale). Follow the closure ritual:
   verify no plan-scoped tests need migrating (plan-12.5 if one does).
2. Rewrite the mixed-controller-local-mode outline status table (plans 1/2/3/4/6 merged; only 5 + 7
   remain); refresh its broken Read-first anchors to the post-split filenames.
3. Reconcile STATUS.md: remove the framework-refactor DRAFTED/Track-1 stanza (it is COMPLETE); prune the
   ~130 lines of superseded IN-PROGRESS time-capsules to the live items (mixed-mode plans 5/7; the rc.5
   soak; owner-owed smokes); add the missing beta16 entry (or fold it if it archived); keep `Recently
   closed subjects` accurate.
4. Update MEMORY.md's `framework-refactor-shipped` pointer to reflect "archived to `_completed/`."
5. Append the 7 missing CHANGELOG footer compare-links (beta.10 … beta.16).

## Tests produced
None (docs/bookkeeping).

## Verification
`git status` shows only moves + doc edits; `ls implementation_plans/_completed/` matches STATUS's
"archived" claims; STATUS.md has no COMPLETE-vs-DRAFTED contradiction; the mixed-mode outline anchors
resolve; CHANGELOG footer links all defined. No code files touched (invariant 4).

## Definition of done
- Six subjects archived; STATUS/MEMORY/CHANGELOG + the mixed-mode outline reconciled and self-consistent;
  a resuming session sees exactly the live work. Review → fix → re-review clean → CI green.

## Out of scope
Any code change. Executing the remaining mixed-mode plans (5/7) — this only fixes their TRACKING.

## Commit template
```
docs(plans): archive six delivered subjects; reconcile STATUS/CHANGELOG/mixed-mode (#PR)

Six delivered subjects (incl. framework-refactor) were never archived; STATUS called
framework-refactor both COMPLETE and DRAFTED with ~130 lines of stale time-capsules;
mixed-controller-local-mode was mis-tracked in three places with pre-split anchors;
CHANGELOG missed 7 footer links. git mv the delivered folders to _completed/, reconcile
STATUS/MEMORY, fix the mixed-mode outline + anchors, and add the compare-links. Docs only.
```

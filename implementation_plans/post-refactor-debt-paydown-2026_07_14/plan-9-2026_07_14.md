# plan-9 — internal/api dedup + handler_bootstrap split + extend the adapter to the agent mux (LOW–MED)

- **Subject:** post-refactor-debt-paydown-2026_07_14 · **Tier:** 2 (structural paydown)
- **Status:** pending · **Branch:** `refactor/api-dedup-and-split`
- **Reads from specs:** `controller-operator-api`, `controller-agent-api`

## Prerequisites
None. (Builds on framework-refactor plan-7's `api/adapter.go` + `errmap.go` on `main`.)

## Goal
Three related api-layer cleanups: (a) split the mis-named `handler_bootstrap.go` (659 LOC — it fuses
Settings + Bootstrap + validators; the name lies); (b) route its local `shQuote` through the
`ShellToken` seam (a shell-safety seam bypass); (c) extend the structural auth-adapter's guarantee to
the agent mux handlers and dedup the residual guard/identity/relay preambles the adapter did not absorb.

## Read first
- `internal/api/handler_bootstrap.go:72-209` (Settings: `settingsResponse`/`loadSettings`/
  `HandleSettings`), `:209-251` (`HandleBootstrap`), `:291-448` (validators + `renderBootstrapScript`),
  `:442` (`shQuote` — the local quoter bypassing `ShellToken`).
- `internal/api/adapter.go` — the structural auth chokepoint (runs `identity()` before dispatch) + how a
  handler opts into it; `internal/api/errmap.go` — the sentinel→code table.
- `internal/api/routes_controller.go:1-408` + `internal/api/handler_agent.go:1-389` — the agent mux
  (does it go through the adapter, or hand-roll the guard+identity+relay preamble?).
- `internal/api/no_anonymous_compute_test.go` + `internal/api/controller_http_test.go` (1221) — the
  gates that must stay green.
- ASSESSMENT.md **T9** + the gap-pass note (`shQuote` seam bypass; the adapter's structural guarantee not
  yet covering the agent mux).

## Implementation steps
1. Split `handler_bootstrap.go` → `handler_settings.go` (Settings handlers + `settingsResponse`/
   `loadSettings`) + `handler_bootstrap.go` (Bootstrap + `renderBootstrapScript` + its validators). Pure
   motion.
2. Route `shQuote` (`:442`) through the `ShellToken` seam — the bootstrap script's `OP_FLAGS` expansion
   is exactly the root-shell field class `ShellToken`/`field_safety_test` exists to type. Extend
   `field_safety_test` to cover the bootstrap-rendered fields.
3. Extend the adapter's structural guarantee to the agent mux: route the agent handlers through the
   adapter (or an agent-mux equivalent) so `identity()`/method-guard is structurally un-skippable there
   too; dedup the residual preambles the adapter did not absorb.

## Tests produced
- **Perpetual:** the extended `field_safety_test` coverage of the bootstrap fields; an adapter
  structural test asserting the agent mux handlers are guarded.

## Verification
`go test ./internal/api/...` (incl. `controller_http_test`, `no_anonymous_compute_test`,
`field_safety_test`); `go vet ./...`; the route table + handler behavior unchanged (diff responses for a
few endpoints); `gofmt -l`.

## Definition of done
- `handler_bootstrap.go` is split (name no longer lies); `shQuote` goes through `ShellToken` (+
  field_safety coverage); the agent mux inherits the adapter's structural guard; preambles deduped;
  gates green. Review → fix → re-review clean → CI green.

## Out of scope
The error-framework design (already landed plan-7). Any route/endpoint behavior change. The FE error
framework (deliberately distinct).

## Commit template
```
refactor(api): split handler_bootstrap; route shQuote through ShellToken; guard the agent mux (#PR)

handler_bootstrap.go fused Settings + Bootstrap + validators (the name lied) and used
a local shQuote that bypassed the plan-6 ShellToken seam; the agent mux did not inherit
the plan-7 adapter's structural identity()/method guard. Split the file, type the
bootstrap shell fields via ShellToken (+ field_safety coverage), and extend the adapter
to the agent mux. Behavior-preserving.
```

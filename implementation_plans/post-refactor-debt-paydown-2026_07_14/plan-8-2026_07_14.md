# plan-8 — Compiler core: split derivePeersWithDomains + extract link-grouping + retire dead orientation (MED)

- **Subject:** post-refactor-debt-paydown-2026_07_14 · **Tier:** 2 (structural paydown)
- **Status:** pending · **Branch:** `refactor/split-derive-peers`
- **Reads from specs:** `compiler-allocation`, `docs/spec/compiler/peer-derivation.md`

## Prerequisites
None. (framework-refactor plan-5b split `peers.go`; this splits the remaining ~580-line function in
`peers_build.go`, which 5b left whole.)

## Goal
`derivePeersWithDomains` (`peers_build.go:58-640`) is a single ~580-line function — the last compiler
god-function. Split it along its existing phases, extract the link-grouping loop the assessment found
duplicated, and delete the dead orientation code. This is the pure core, so it is **hard-gated**: the
output must stay byte-identical.

## Read first
- `internal/compiler/peers_build.go:20-58` — `DerivePeers` → `derivePeers` → `derivePeersWithDomains`
  call chain (the seam to split behind).
- `internal/compiler/peers_build.go:58-640` — the function to split (identify its internal phases:
  pre-allocation, per-link grouping, PeerInfo construction, write-back).
- `internal/compiler/peers_build.go:641-863` — the already-extracted helpers (`allocateTransitPair`,
  `gapFill*`, `deriveLinkCost`) to model the new helpers on.
- `internal/compiler/allocation_stability_test.go` (682) + `internal/compiler/peers_endpoint_test.go`
  (541) — the byte-stability gates.
- ASSESSMENT.md **T12** (the split + the duplicated link-grouping iterator + the dead orientation code
  to retire — CONFIRM the orientation code is unreferenced before deleting: framework-refactor kept
  `edgecase.Compile` for test callers, so confirm-before-delete).

## Implementation steps
1. Extract named helpers for each phase of `derivePeersWithDomains` (e.g. `groupLinks`, `buildPeerInfo`,
   `stampWriteBack`) — pure motion, no logic change.
2. Extract the duplicated link-grouping loop into ONE iterator/helper and route the call sites through
   it (the assessment found it 3×).
3. Delete the dead orientation code — but CONFIRM zero callers first (grep production + tests); if a test
   references it, keep it or move it to a test helper (the framework-refactor confirm-before-delete
   discipline).

## Tests produced
- None new required (the split is gated by the existing corpus); add a focused unit test only if a
  newly-extracted helper has a non-obvious contract worth pinning.

## Verification
`export PATH="$HOME/.local/go/bin:$PATH" GOTOOLCHAIN=local`; `go test ./internal/compiler/...` (incl.
`allocation_stability_test`); `go test ./internal/localcompile/... -run Golden` (byte-identical);
`GOOS=js GOARCH=wasm go build ./cmd/wasm` + `node scripts/wasm-conformance-gate.mjs` (WASM==golden stays
green); `go vet ./...`; `gofmt -l`. **Any golden byte-diff is a split bug — never regenerate the
fixture** (stop-loss → plan-8.5).

## Definition of done
- `derivePeersWithDomains` is decomposed into named helpers; the link-grouping loop is single-sourced;
  dead orientation code removed (confirmed unreferenced); every golden + allocation-stability + WASM
  conformance gate byte-green. Review → fix → re-review clean → CI green.

## Out of scope
Any allocation semantics change. `Node.ID` identity (deferred). The already-clean helpers below `:640`.

## Commit template
```
refactor(compiler): split derivePeersWithDomains; single-source link grouping (#PR)

derivePeersWithDomains was a ~580-line god-function (the last in the pure core). Split
it into named per-phase helpers, extract the 3x-duplicated link-grouping loop into one
iterator, and delete the confirmed-dead orientation code. Byte-identical: every golden,
allocation-stability test, and the WASM conformance gate stay green.
```

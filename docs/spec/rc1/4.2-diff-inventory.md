# 4.2 — Diff-aware security inventory (BASE..HEAD)

> plan-21 (4.2) Task 1. The captured baseline + the per-file change classification + the beta.8/plan-8
> anchor-presence FAIL-early check. The adversarial verdict is in [`4.2-verdict.md`](./4.2-verdict.md).

## BASE (captured once)

**`BASE = v2.0.0-beta.8` annotated tag → commit `e765da1`** (the M0 baseline-freeze floor; the
CHANGELOG-roll commit). Resolved: `git rev-list -n1 v2.0.0-beta.8` = `e765da1823b554d9f35ab0495d2f0874b55b32a6`.
**NOT `c335be0`** (the pre-merge beta.8 fix commit — using it would fold beta.8's own post-fix churn +
the `internal/regression` keystone suite into the diff). `HEAD` = the frozen post-Subjects-1/2/3 tree
(`sec/diff-aware-sweep` off `main`). Single ref; no dual baseline.

## Security file set — change class (BASE..HEAD)

| file | Δ (ins/del) | class | beta.8/plan-8 anchor → status |
|------|-------------|-------|-------------------------------|
| `internal/api/server.go` | +62/−60 | rewritten | **B1** `recovered(mux)` wraps BOTH muxes (`:186`, used by `Handler()`/`AgentHandler()`) — present; plan-7 also tags the air-gap registration here |
| `internal/api/cookie_session.go` | **unchanged** | — | **S10** `csrfValid` double-submit (`:97`) — confirmed-by-reference (byte-identical to the beta.8-audited state) |
| `internal/api/loginratelimit.go` | **unchanged** | — | **S9** per-username lockout + break-glass — confirmed-by-reference |
| `internal/controller/enrollment.go` | **unchanged** | — | **S4** `ErrNodeRevoked`/`reenrollApproved` (`:168-182,238`) — confirmed-by-reference |
| `internal/controller/memstore.go` | **unchanged** | — | **S5** `PurgeEnrollmentTokensForNode` (`:522`) — confirmed-by-reference |
| `internal/agent/verify.go` | **unchanged** | — | **TOFU** policy comment (`:130-146`) — confirmed-by-reference (DOCUMENT decision unchanged) |
| `internal/api/handler_passkey.go` | +11/−0 | additive | **B3** login Origin required (`:236-246`) — present |
| `internal/api/handler_bootstrap.go` | +73/−10 | extended | bootstrap arg-injection: `validateOperatorCredentialBinding` (`:308`); `${OP_FLAGS}` intentionally unquoted (`:300-302`) — present |
| `internal/api/handler_deploy.go` | +689/−0 | new content | **S5** purge call (`:349`), **S6** TTL clamp `7*24*60*60` (`:566-567`) — present (relocated from `handler_controller.go`) |
| `internal/controller/filestore.go` | +60/−10 | extended | **B2** `writeJSONAtomic` fsync; **S5** `PurgeEnrollmentTokensForNode` (`:1259`) — present |
| `internal/api/release_pins.go` | +58/−19 | extended | **S7/S8/S11** `blockPrivateAddr` (`:52`), `embeddedIPv4` (`:98-111`), `192.0.0.0/24` deny (`:84-89`) — present |
| `internal/allocator/ip.go` | +85/−29 | extended | **S1** host-space cap + ctx (oracle/CLI surface only after plan-7) — present |
| `internal/validator/schema.go` | +133/−75 | rewritten | **S2** topology caps (`topologyExceedsBounds`) — present |
| `internal/compiler/peers.go` | +399/−300 | rewritten | **S3** gap-fill per-pool cursor + ctx (oracle/CLI only); **C3** consumes `roles.go` HasPublicIP normalization — present |
| `internal/renderer/babel.go` | +70/−57 | rewritten | **C1** peer slice sorted by `InterfaceName` (`:128-132`) — present (compare peers as a SET keyed by `InterfaceName`, never PeerMap slice order) |
| `frontend/src/api/controllerClient.ts` | +67/−45 | extended | **F1** `getTrustlist` via shared `request()` (`:993-995`) — present |

**NEW since BASE:** `internal/api/airgap_routes.go` (`//go:build airgap` — the compute routes plan-7 moved
behind the tag, route-absent on the shipped controller), `internal/api/airgap_routes_removed_test.go`
(plan-7's 404 negative test), `frontend/src/compiler/keygen.ts` + `export.ts` (the plan-4 TS-port — N1).

## FAIL-early anchor check — RESULT: PASS

Every beta.8 anchor (B1/F1/C1/S4/S5/S6) and every plan-8 anchor (S1–S3, S7/S8/S11, B2, bootstrap
arg-injection, C2/C3) is **present or equivalently relocated** on `HEAD` (the relocations — S5/S6 →
`handler_deploy.go`; route registration/`operatorAuth` → `routes_controller.go`/`auth_controller.go` —
are documented in `4.1-reaudit-charter.md`). No guard moved without an equivalent replacement, so the
static prong proceeds (it does not abort). The five **byte-unchanged** files (cookie_session,
loginratelimit, enrollment, memstore, verify) are confirmed-by-reference to the beta.8-audited state —
a zero-diff surface with a present guard is not re-walked line-by-line (diff-aware discipline).

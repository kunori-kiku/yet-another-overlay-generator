# 4.2 — Diff-aware adversarial sweep: VERDICT

> plan-21 (4.2) Task 8 — the **SUBJECT-4 security sign-off**. An INPUT to plan-22 / 4.3 criterion **A2**
> ("zero new confirmed blockers") and the [4.1 charter](./plans/4.1-reaudit-charter.md) exit bar; it does
> NOT itself gate the tag (plan-22 owns it). Baseline: [`4.2-diff-inventory.md`](./4.2-diff-inventory.md)
> (BASE = `v2.0.0-beta.8` / `e765da1`).

## VERDICT: **GO**

Zero open HIGH. Zero unmitigated MEDIUM on the new surfaces N1–N4. The two NEW completeness gaps the
sweep found (both LOW) were **fixed inline** (the plan's "one-line/obvious hardening" allowance); the one
charter-disposition error it found (S3) is recorded as an **accepted residual** (LOW, deployed-surface
closed-by-removal; the code's own DoS oracle already defers it post-rc.1). Every beta.8 + plan-8 baseline
guard is present and load-bearing (static + live-wire).

## Findings (3 confirmed, 0 refuted, 0 uncertain)

| # | surface | sev | kind | disposition |
|---|---------|-----|------|-------------|
| 1 | O3 SSRF | LOW | new | **FIXED** — `embeddedIPv4` now unwraps the SIIT/RFC 6052 `::ffff:0:0/96` form (`release_pins.go`), with regression cases in `release_pins_test.go`. |
| 2 | N1 TS-port | LOW | new (parity) | **FIXED** — `export.ts` now applies `validateSafeName` before using a node name as a ZIP path (mirrors Go's `internal/artifacts/export.go`); vitest `export.zipslip.test.ts`. |
| 3 | O5 compile-DoS | LOW | charter-correction | **ACCEPTED RESIDUAL** — S3 gap-fill quadratic is NOT fixed (the charter wrongly said "DONE plan-8"); it is post-rc.1 roadmap per the code's own DoS oracle. Deployed-surface reachability closed-by-removal + operator-auth (below). |

### 1 — SSRF SIIT `::ffff:0:0/96` (LOW, NEW) → FIXED

`embeddedIPv4` unwrapped 6to4 / NAT64 / IPv4-compat but missed the SIIT IPv4-translatable prefix
(`::ffff:0:169.254.169.254` → loopback/RFC1918/metadata all passed `isPublicUnicastIP` as "public").
Blast radius near-zero on the shipped posture (operator-auth-gated route; literal bracketed-IPv6 base
URLs already rejected by `validateReleaseURL`; a stock kernel does not route `::ffff:0:0/96` to internal
v4 without a configured SIIT translator). Still a real completeness gap relative to its sibling unwraps,
so **fixed**: a `::ffff:0:0/96` branch in `embeddedIPv4` + four `release_pins_test.go` table cases (SIIT
loopback/metadata/RFC1918 → refused; SIIT of public 8.8.8.8 → still allowed, no over-block).

### 2 — TS exporter zip-slip (LOW, NEW, N1 parity) → FIXED

The plan-4 TS exporter dropped Go's export-time `validateSafeName` guard: `export.ts` used `node.name` as
a JSZip directory unsanitized, and the name-charset validator allows `.`, so a node named `..` passed
validation. Bounded + operator-local (export hard-refuses in controller mode; the ZIP never reaches
controller storage), but it violated the **N1 invariant** (the browser exporter must not be
security-weaker than Go). **Fixed**: ported `validateSafeName` into `export.ts` (rejects ``/`.`/`..`/
path-separators/`..`-containing names), with `export.zipslip.test.ts` proving the charset-valid-but-unsafe
names (`..`, `.`, `a..b`) are rejected and safe names still export.

### 3 — S3 compile-DoS gap-fill quadratic (LOW) → ACCEPTED RESIDUAL + charter correction

The 4.1 charter's baseline row recorded S3 as "DONE plan-8 (oracle): per-pool cursor + ctx in peers.go
gap-fill." **That guard does not exist** — `internal/compiler/peers.go` takes no `ctx`, and
`gapFillTransitPair`/`gapFillLinkLocalPair`/`lowestFreePort` restart their scan from 0 per link (O(L²)).
A valid 9999-edge topology with a `/12` transit CIDR pins a core ~17s. The codebase's OWN DoS oracle
agrees (`internal/edgecase/dos_repro_test.go`: "S3 hardening is post-rc.1 roadmap (4.3:48)"; it asserts
only boundedness, not a cap). So the charter's disposition was wrong; this verdict corrects it (and the
charter's S3 row is amended to match).

**Why it is an accepted residual, not a blocker:**
- The anonymous `/api/compile` route is `//go:build airgap` — **route-absent (404) on the shipped
  controller** (verified, Task 6). The only unauth reach is the `-tags airgap` oracle / `cmd/compiler`: a
  single local user, self-inflicted, LOW.
- On the shipped controller, the compile path is **operator-authenticated** (`HandleCompilePreview`/
  `HandleStage`) and `enrolledSubgraph` filters edges to enrolled-node pairs — a trusted actor, bounded.
- S3 is on the **post-rc.1 hardening roadmap** by the program's own design; the cap (per-pool cursor +
  ctx threading through `peers.go`) is a non-trivial algorithmic change beyond this sweep's
  report-don't-rewrite scope. Recorded for the owner; not a tag blocker.

## Baseline regression matrix (beta.8 + plan-8 — verify, don't rediscover)

| finding | static anchor present | live-wire / dynamic | status |
|---------|-----------------------|---------------------|--------|
| **B1** panic recovery (both muxes) | `server.go:186` `recovered(mux)` | in-process `beta8_blockers`/server tests (live panic needs a seam absent from the release binary) | un-regressed |
| **F1** getTrustlist via `request()` | `controllerClient.ts:993-995` | `deploy-keystone.spec.ts` (keystone-sign Deploy → getTrustlist 200, no 401) | un-regressed |
| **C1** babeld.conf byte-stability | `babel.go:128-132` sort by `InterfaceName` | conformance babeld golden (plan-5) | un-regressed |
| **S4** revoked-resurrection guard | `enrollment.go:168-182,238` (file byte-unchanged) | **`internal/dast` `TestDAST_RevokeBlocksTokenResurrection` (live wire)** | un-regressed |
| **S5** purge-on-revoke | `handler_deploy.go:349` + both stores | **`internal/dast` live wire** (above) | un-regressed |
| **S6** enroll-token TTL cap | `handler_deploy.go:566-567` | **`internal/dast` `TestDAST_EnrollmentTokenTTLCapped` (live wire, over-cap → 400)** | un-regressed |
| **S7/S8/S11** SSRF completeness | `release_pins.go` `embeddedIPv4` + `192.0.0.0/24` | `release_pins_test.go` table (+ new SIIT cases) | un-regressed (+ SIIT gap closed) |
| **S12** SSRF guard sound | `release_pins.go:52` post-DNS hook | table | un-regressed |
| **S1** allocator cap+ctx | `allocator/ip.go` | in-process `server_limits` + edgecase DoS oracle (airgap-only reach) | un-regressed (oracle surface) |
| **S2** topology caps | `validator/schema.go` `topologyExceedsBounds` | schema tests | un-regressed |
| **S3** gap-fill quadratic | — (NOT fixed) | edgecase DoS oracle (bounded, not capped) | **accepted residual (finding 3)** |
| **B2** FileStore fsync | `filestore.go` `writeJSONAtomic` | static-anchor + crash-reasoning | un-regressed |
| **B3** passkey login Origin | `handler_passkey.go:236-246` | login matrix (plan-14) | un-regressed |
| **bootstrap arg-injection** | `handler_bootstrap.go:308` `validateOperatorCredentialBinding` | bootstrap tests | un-regressed |
| **B4** export-ZIP amplification | — | CLOSED-BY-REMOVAL (plan-7: `/api/export` `//go:build airgap`, 404 on controller) | closed-by-removal |
| **S9 / S10 / TOFU** | `loginratelimit.go` / `cookie_session.go:97` / `verify.go:130-146` (all byte-unchanged) | DOCUMENT decisions — code matches the recorded boundary | un-regressed |

## New-surface (N1–N4) + plan-7 disposition

- **N1 (TS client trust boundary):** the controller re-validates browser-supplied topologies server-side
  (no browser value reaches a trusted sink un-revalidated) — invariant holds. Keygen uses
  `crypto.getRandomValues` + `@noble/curves` x25519 (not `Math.random`), pinned by the plan-5 keygen KAT.
  The one parity gap (zip-slip) is **fixed** (finding 2).
- **N2 (controller-only backend):** the anonymous compute/keygen routes are `//go:build airgap`,
  **404 on the shipped controller** — verified by `TestAirgapRoutes_NotRegisteredInDefaultBuild` +
  `TestControllerHTTP_AirGapOpen` (Task 6). Static-SPA local deployment ships no backend listener.
- **N3 (Subject-2 phone UX):** frontend-only (plans 10–12 add no `/api/` route, no new `localStorage`
  secret write, no new dep) — confirmation holds (no fresh audit needed).
- **N4 (TS crypto/ZIP supply chain):** `npm audit --omit=dev` = **0 vulnerabilities** over
  `@noble/curves`+`@noble/hashes`+`jszip`. Now CI-scanned (advisory).

## CI security tooling (institutionalized — the #1 round-1 gap)

| scan | scope | status | CI |
|------|-------|--------|-----|
| `govulncheck ./...` (+ `-tags airgap`) | Go reachable vulns | **0 affecting code** (18 in required modules, not called) | REQUIRED (`security-scan` job) |
| `npm audit --omit=dev` | frontend prod supply chain | **0 vulnerabilities** | ADVISORY |
| `gosec -conf .gosec.json` | Go SAST | baseline **61** findings (G304 file-path-from-var ×18, G306 ×9, G101 ×6, G115 ×5, … — inherent to a config tool; reviewer diffs against this baseline) | ADVISORY |
| `internal/dast` live-wire replay | S4/S5/S6 at the wire | both cases PASS | REQUIRED |

plan-22 / 4.3 ratifies the required/advisory toggles in `RC1-GATE.md`. `-race` is plan-22's (`go` job).

## Coverage boundary (so "GO" is scoped, not absolute)

- **Probed:** the diff `BASE..HEAD` security file set (static threat re-derivation), the beta.8 + plan-8
  guards (static anchor + live-wire DAST where wire-reachable), the SSRF table, the TS-port new surface
  (keygen/zip-slip/validator-parity), the plan-7 route removal, and the dep/SAST supply chain.
- **Confirmed-by-reference (zero diff vs beta.8):** `cookie_session.go`, `loginratelimit.go`,
  `enrollment.go`, `memstore.go`, `verify.go` — byte-unchanged from the beta.8-audited state.
- **Not probed (out of scope / delegated):** the deployment substrate (TLS proxy, host OS, Docker); the
  irreducible owner-run hardware smokes (`RUNBOOK.md` §C1/§C2/§C3); browser-session security beyond the
  plan-13–17 E2E suite + the new live-wire DAST; concurrency races (plan-22 `-race`).
- **Browser `@security` specs:** the 4.1-named probes are covered by the existing E2E suite — F1 by
  `deploy-keystone.spec.ts` (getTrustlist 200 on keystone-sign), CSRF/auth-bypass/lockout by the
  in-process `cookie_session`/`loginratelimit` tests + the plan-14 login matrix, enrollment exploits by
  the NEW `internal/dast` live-wire driver. A dedicated `frontend/e2e/security/` `@security` tag grouping
  (incl. a refreshed-cookie-then-keystone-sign variant) is a recommended follow-up, not a gap in coverage.

## Residual-risk register

| risk | severity | classification |
|------|----------|----------------|
| S3 gap-fill quadratic (oracle/operator-auth reach) | LOW | **accepted-with-rationale** — post-rc.1 roadmap; deployed surface closed-by-removal + operator-auth |
| gosec advisory baseline (61 pre-existing findings) | LOW | accepted — config-tool-inherent (G304/G306); advisory, reviewer-diffed |
| 18 govulncheck module-level vulns (not called) | INFO | accepted — not reachable; `govulncheck` REQUIRED catches any that become reachable |

*Cross-links:* [`4.1-reaudit-charter.md`](./plans/4.1-reaudit-charter.md) (exit bar — this verdict is its
evidence), [`4.2-diff-inventory.md`](./4.2-diff-inventory.md) (baseline), `RC1-GATE.md` (criterion A2 —
this verdict feeds it), [`RUNBOOK.md`](./RUNBOOK.md) (the owner-run residue this sweep does not cover).

# 3.4 — Edge-case & adversarial-usage hunt: findings ledger

> plan-16 deliverable. Every finding below has a runnable reproduction (a named test/spec) and an
> owning subject. IDs in `code font` are the canonical investigation IDs from
> `docs/spec/rc1/investigation-findings.json` (`rc1_blockers` / `post_rc1_roadmap`); the `F-3.4-*`
> IDs are findings this hunt surfaced that were not pre-catalogued.
>
> Engines: **Engine B** = `internal/edgecase/` (Go generator + corpus + fuzz + DoS oracle, test-only,
> beside `internal/regression`); **Engine A** = `frontend/e2e/adversarial/` (Playwright fault
> injection on the plan-13 rig).
>
> Key reconciliation: the program order ran Subject-1 plan-8 (1.8) BEFORE this plan, so the S1 DoS
> cap+context AND the C2 re-enable auto-heal are ALREADY SHIPPED. This plan therefore turns the
> "pre-fix repro" the plan envisioned into a **regression lock on the shipped fix** for S1 and C2 —
> exactly the "single helper the fix flips" the plan describes, with the fix already flipped.

## Severity legend

`blocker` rc.1 cannot ship · `high` fix before rc.1 if cheap · `medium` post-rc.1 roadmap ·
`low` informational / defense-in-depth confirmed · `fixed` resolved + regression-locked here or upstream.

## Findings

| ID | Sev | Reproduction (test/spec) | Owner | Status & fix direction |
|----|-----|--------------------------|-------|------------------------|
| `F-3.4-1` deploy()/saveDesign() re-entrancy | fixed | `frontend/e2e/adversarial/double-submit.spec.ts` | **plan-16 (here)** | **FIXED here.** The Deploy/Save buttons disable while in flight, but a synthetic click bubbles to React's delegated `onClick` even on a disabled button and re-invoked the store action, issuing a SECOND `update-topology` POST (confirmed: count=2 pre-fix). Landed the owner-pre-authorized idempotency early-return guard (`if (get().loading) return` / `if (get().saving) return`) at the top of `deploy()` / `saveDesign()` (controllerStore.ts). The spec now asserts a single effective POST. |
| `F-3.4-2` WG interface-name hash collision at scale | low | `internal/edgecase/dos_repro_test.go::TestDegenerateBackupInterfaceCollision` | Subject-1 compiler (informational) | Parallel backup links to the SAME peer derive their WG interface name from one base + a short hash whose namespace collides by ~16 links; the semantic validator then rejects the topology LOUD ("rename the colliding node"). Bounded + coded — never a silent interface overwrite, never a hang. Effectively caps per-peer parallel-backup fan-out well below the edge-count bound. No fix needed; recorded so a future "raise the fan-out" request knows the namespace is the limit, and so the loud-fail is regression-locked. |
| `allocator-cidr-reserved-cpu-blowup` (S1) | fixed | `internal/edgecase/dos_repro_test.go::TestDoSAllocatorScanBudget` + `TestDoSAllocatorContextHonored`; HTTP tier `dos_repro_http_test.go::TestDoSHTTPScanBudget` | plan-8 (1.8) | **FIXED upstream (plan-8), regression-locked here.** `internal/allocator/ip.go` caps the per-node host scan at `maxOverlayScanBudget = 1<<20` (a `/8` is rejected up front with `compile_overlay_scan_budget_exceeded`, 422) and polls `ctx.Err()` every 4096 candidates so a long in-budget scan is abortable. Was the hard rc.1 DoS blocker; now closed. The oracle helper is in its post-fix "assert bounded coded error" state. |
| `unbounded-domains-and-reserved-ranges` (S2) | medium | `internal/edgecase/dos_repro_test.go::TestDoSUnboundedDomains`; HTTP tier `TestDoSHTTPUnboundedDomains` | post-rc.1 roadmap | Domain/reserved-range counts are bounded only by the 4 MiB body cap (not the node/edge schema bounds). Measured bounded (500 domains × 8 reserved + 1 node each compiles in ~7 ms / HTTP ~150 ms). The oracle asserts BOUNDED (no hang) today; flip it to require a coded cap when the post-rc.1 domain/reserved-count bound lands. Not an rc.1 blocker. |
| `peers-gapfill-quadratic-parallel-links` (S3) | medium | `internal/edgecase/dos_repro_test.go::TestDoSGapFillAtScale` | post-rc.1 roadmap | `gapFillTransitPair` re-`ParseCIDR`s per iteration; the cost is super-linear in links. Reachable at scale via many DISTINCT links (a large star), NOT via parallel backups to one pair (those hit `F-3.4-2`'s interface-name cap first). Measured bounded (1000-link star, /16 transit, ~170 ms) under the 2000-node schema cap. Oracle asserts BOUNDED today; flip when post-rc.1 hardening lands. Not an rc.1 blocker. |
| C2 re-enable cross-link pin collision | fixed | `internal/edgecase/c2_reenable_test.go` (`TestC2RawCollisionRejectedLoud` + `TestC2HealRepairsThenCompiles`) | Subject-1 compiler (plan-8 Phase 6.3) | **FIXED upstream, regression-locked here.** A re-enabled stale edge can leave two different links pinning the same transit IP. `normalize.HealCollidingPins` repairs it (on save / deploy / canvas load); the compiler's semantic validator remains the loud safety net (`validation_pin_transit_ip_duplicate_cross_link`) when an un-healed corruption reaches it. This plan locks BOTH halves through the public compile path. Canonical heal semantics: `internal/compiler/reenable_heal_test.go` + `internal/validator/allocation_pins_test.go`. |
| `per-username-login-lockout-dos` (S9) | medium (decision) | `frontend/e2e/adversarial/error-render.spec.ts` (login 429, EN+ZH) | plan-8 (1.8) decision | Repro/positive-contract only: the panel correctly RENDERS the localized `auth_rate_limited` 429 message. The limiter exists (`maxLoginFailures = 10`, per-username + per-IP); whether the lockout policy is accepted as-is for rc.1 is plan-8's DECISION. The 429 is fault-injected (not driven by 10 real failures) to avoid locking out the shared operator/IP and contaminating other specs. |
| `csrf-token-not-bound-to-session-server-side` (S10) | medium (decision) | `frontend/e2e/adversarial/error-render.spec.ts` (CSRF positive contract) | plan-8 (1.8) decision | Positive contract only: a state-changing cookie-auth request with no `X-CSRF-Token` is rejected (403 `req_csrf_invalid`). Bearer auth is CSRF-exempt by design (`auth_controller.go`). Whether the double-submit token must additionally be bound to the session server-side is plan-8's DECISION; this plan proves the present positive contract holds. |
| B1 mux panic recovery | fixed (beta.8) | `internal/edgecase/fuzz_test.go::FuzzCompile` (no-panic invariant) | canonical: plan-22 (4.3) | Shipped in `v2.0.0-beta.8` (`recoverPanics` on both muxes → coded 500, not a torn connection). This plan adds COMPLEMENTARY stress (the fuzz no-panic property over the compile path); the canonical `CodeInternalPanic` handler pin is plan-22's. |
| C1 babeld byte-stability under edge reorder | fixed (beta.8) | `internal/edgecase/fuzz_test.go::FuzzCompile` (order-independence invariant) | canonical: plan-5 (1.5) golden | Shipped in beta.8 (`internal/renderer/babel.go` peer sort). This plan adds a REGRESSION LOCK (reorder the edge slice → byte-identical babeld.conf) over the fuzz corpus; canonical cross-language byte-equality is plan-5's conformance golden (no duplicate oracle). |
| F1 getTrustlist cookie-auth routing | fixed (beta.8) | (owned elsewhere) | canonical: plan-15 (3.3) | Shipped in beta.8 (`getTrustlist` routes through the shared `request()` helper; 404→keystone-OFF). 3.3's `keystone-rotation.spec.ts` + 3.2's `deploy.spec.ts` own the `page.reload()` regression. This plan adds only the DISTINCT stale-tab angle (`stale-tab.spec.ts`: in-memory bearer cleared without reload → cookie auth; expired cookie → re-login), explicitly non-overlapping. |

## Deploy fault-matrix coherence (verified contracts, not defects)

`frontend/e2e/adversarial/deploy-faults.spec.ts` confirms `deploy()` leaves coherent state under a
fault at each step — these PASSED, i.e. the orchestration is already correct:

- fault at `update-topology` (torn connection) → error surfaced, Deploy re-enabled, `stage`/`promote` never called.
- fault at `getTrustlist` (500, not 404) → deploy aborts BEFORE `promote` (no half-promoted fleet).
- `getTrustlist` 404 on keystone-OFF → `promote` proceeds, deploy completes (the explicit positive contract).
- fault at `promote` (500) → error surfaced, deploy does not report success, button re-enabled.
- fault at the post-`promote` reconcile `topology` GET → deploy STILL reports success (best-effort reconcile).

## Charset / degenerate observations (defense confirmed)

- Unicode / emoji node names are REJECTED at schema validation (shell-metacharacter safety) —
  `internal/edgecase/` corpus `unicode-names`, asserted by `TestCorpusCompilesOrCodedError`. Confirmed
  defense, not a defect.
- Empty / single-node / disconnected / all-edges-disabled / self-loop topologies all compile or
  reject cleanly (no panic, no `(nil, nil)`) — corpus `degenerate` class.

## What this plan changed in production code

- `frontend/src/stores/controllerStore.ts` — the two idempotency early-return guards (`F-3.4-1`),
  each proven by `double-submit.spec.ts`. No other behaviour change.
- `frontend/src/components/shell/E2ERenderThrowProbe.tsx` + `App.tsx` + `vite-env.d.ts` — a
  build-flag-gated (`import.meta.env.VITE_E2E`) test-only render-throw seam for the ErrorBoundary
  spec; dead-code-eliminated from every production bundle (verified: absent from a no-flag build).

Everything larger (the S2/S3 caps, any deeper store refactor) is FILED above, not changed here.
